# Architecture

## 사용자 흐름

```text
이미지/폴더 추가
→ 번역 시작
→ OCR
→ Vision LLM: OCR 검수 + 번역
→ 인페인트
→ 한글 조판
→ 저장
```

## 원칙

- StoneCandy 코드에 의존하지 않는 새 구현
- UI는 VOSR처럼 단순하게 유지
- 모델은 폴더/manifest 기반으로 교체 가능
- 모델 다운로드와 실제 처리 상태는 하단 작업 로그에 계속 표시
- 대형 모델 바이너리는 Git에 포함하지 않음

## 현재 구현 범위

- WPF 다크 UI
- 드래그 앤 드롭
- 파일/폴더 추가
- 전체 삭제
- 저장 폴더 선택/열기
- 작업 큐
- 진행률/중지
- 작업 로그
- 모델 manifest 자동 탐색
- 기존 RapidOCR/PP-OCRv5 다중 패스 OCR

## 다음 단계

1. Ollama 상태/모델 설치 확인
2. Qwen Vision에 OCR 결과 + 이미지 전달
3. 구조화 JSON으로 OCR 교정문 + 한국어 번역 수신
4. 인페인트
5. 한글 조판
