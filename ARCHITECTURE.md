# Architecture

## 사용자 흐름

이미지/폴더 추가
→ OCR
→ Vision OCR 검수
→ 최종 한국어 번역
→ 인페인트
→ 한글 조판
→ 결과 저장

## 원칙

- StoneCandy 코드에 의존하지 않는 새 구현
- UI는 VOSR처럼 단순하게 유지
- 모델은 폴더의 model.json 프로필로 교체 가능
- 단계별 모델을 분리해서 선택 가능
- 대형 모델 바이너리는 Git에 포함하지 않음
- 모델 다운로드/처리 상태는 하단 작업 로그에 표시
- 일반 커밋은 빠른 컴파일 검사만 수행
- 포터블 Windows 빌드는 필요할 때 수동 실행

## 현재 단계 분리

1. OCR
   - PP-OCRv5 / RapidOCR
   - LLM과 독립
2. Vision OCR 검수
   - 이미지와 OCR 블록을 함께 보고 오인식 교정
   - `review` task
3. 최종 번역
   - 교정된 원문을 기준으로 자연스러운 한국어 생성
   - `translation` task
   - 이미지 입력이 없는 텍스트 전용 모델도 사용 가능
4. 인페인트
   - 독립 모듈
5. 조판
   - 독립 모듈

검수/번역 기본 모델은 `gemma4:12b`이며, 각 단계에 서로 다른 모델을 선택할 수 있습니다.

## 구현됨

- WPF 다크 UI
- 이미지/폴더 드래그
- 파일/폴더 추가, 전체 삭제
- 저장 폴더 선택/열기
- 작업 큐, 진행률, 중지
- 작업 로그
- 역할별 모델 manifest 자동 탐색
- RapidOCR/PP-OCRv5 다중 패스 OCR
- Ollama 서버/모델 설치 상태 확인
- Ollama 모델 다운로드 진행 로그
- Vision 모델의 OCR 교정 + 문맥 검수
- 별도 번역 모델의 최종 한국어 번역
- 페이지별 translation.json 저장
- 커밋용 빠른 Windows 빌드 Action
- 수동 포터블 빌드 Action

## 다음

1. Gemma 4 12B 실제 만화 샘플 품질/VRAM 검증
2. 검수 모델과 번역 모델 조합 비교
3. 컨테이너 감지/보존 조판 실이미지 튜닝
4. 실제 인페인트 모델 연결
5. 번역 품질 고도화


## 조판/인페인트

현재 렌더 단계는 다음 순서로 동작합니다.

1. OCR 블록 주변에서 edge/contour 기반으로 말풍선 또는 캡션 컨테이너 탐지
2. 컨테이너 색상과 관계없이 OCR 글자 영역의 전경 픽셀만 마스크
3. 컨테이너 외곽선을 보호한 상태로 작은 마스크만 인페인트
4. 컨테이너 내부 안전영역(inner rect) 계산
5. 한국어 번역의 강제 [BR]을 soft wrap으로 바꾸고 자동 줄바꿈
6. 박스 높이에 맞춰 폰트 크기와 줄간격 자동 조절
7. 밝은 배경은 검정 글씨, 어두운 배경은 흰 글씨로 조판

컨테이너를 찾지 못한 영역은 OCR 블록을 보수적으로 확장한 fallback 영역을 사용합니다.


### Flood-fill 말풍선 마스크

조판 단계는 OCR 사각형을 그대로 확대하지 않습니다. OCR 블록을 중심으로 주변 영역을 확장한 뒤 Canny 경계와 중심 flood-fill을 사용해 실제 말풍선/캡션 내부 마스크를 추정합니다. 감지에 성공하면 원문 제거와 조판 모두 이 마스크 내부에서만 수행하고, 외곽선을 보호하기 위해 마스크를 소폭 erode합니다. 조판 영역은 안전 마스크 안의 가장 큰 내접 사각형을 사용합니다. 감지에 실패하면 OCR 영역을 소폭 확장한 보수적 fallback만 사용합니다.
