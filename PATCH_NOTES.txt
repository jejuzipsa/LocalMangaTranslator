LocalMangaTranslator - 누적 패치 노트 / 작업 인수인계
============================================================

문서 목적
------------------------------------------------------------
이 파일은 LocalMangaTranslator의 장기 작업 기록과 다음 채팅/작업 세션 인수인계를 위한
누적 패치 노트다.

앞으로 새 버전을 작업할 때마다 이 파일 하나에 버전 번호를 추가하고,
아래 항목을 가능한 한 상세히 기록한다.

- 왜 수정했는가
- 실제로 어떤 문제가 관찰되었는가
- 원인을 어떻게 판단했는가
- 어떤 코드/파이프라인을 변경했는가
- 무엇은 의도적으로 변경하지 않았는가
- 어떤 디버그 파일/audit 필드가 추가되었는가
- 어떤 테스트로 회귀를 잠갔는가
- 실제 이미지 테스트 결과는 어땠는가
- 아직 남은 문제와 다음 우선순위는 무엇인가

새 채팅에서 작업을 이어갈 때는 이 파일, PIPELINE_V2.md,
docs/PIPELINE_REFACTOR.md를 먼저 읽으면 현재 상태를 빠르게 복원할 수 있다.


프로젝트
------------------------------------------------------------
프로젝트명: LocalMangaTranslator
GitHub: https://github.com/jejuzipsa/LocalMangaTranslator
주 작업 브랜치: rewrite/pipeline-v2

목표:
Windows용 로컬 만화 이미지 번역기.
OCR -> Vision OCR 검수 -> 번역 -> 말풍선/캡션 검출 -> 원문 삭제 ->
배경복원 -> 검수 -> 한국어 조판까지 자동 처리한다.

현재 개발 우선순위:
번역 문장 품질 자체보다 이미지 처리 파이프라인의 정확도와 안전성이 우선이다.
특히 OCR/검출 -> 원문 삭제 -> 배경복원 -> 검수 -> 조판의 구조적 안정성을 먼저 확보한다.


핵심 불변 원칙
------------------------------------------------------------
1. RT-DETR Bubble / TextBubble / TextFree geometry는 immutable evidence로 취급한다.
2. OCR/번역/후처리는 RT-DETR이 만든 erase geometry를 이동하거나 새로 만들지 않는다.
3. OCR output은 observation이지 authority가 아니다.
4. Translation approval과 erase approval은 같은 의미가 아니다.
5. 잘 지운 원문을 검수 오판으로 다시 살리지 않는다.
6. Layout 실패 또는 erase 검수 실패 시 해당 unit은 원본 픽셀을 보존한다.
7. erase + typeset은 atomic commit으로 묶는다.
8. 글자를 단순히 지우는 것보다 글자 뒤에 원래 있어야 할 배경을 복원하는 것이 핵심이다.
9. Flat Bubble/Caption과 Artwork/복잡 배경은 복원 전략을 분리한다.
10. 일반 대사/캡션 안정성을 깨면서 stylized text 하나에 과적합하지 않는다.
11. RapidOCR 영역진단 및 보조 OCR evidence는 계속 유지한다.
12. 애매한 경우에는 artwork 손상보다 원본 보존을 우선한다.
13. 디버그/audit 결과가 실제 최종 commit 상태와 분리되지 않도록 한다.


============================================================
이전 작업 요약 - 0001 ~ 0018
============================================================

초기 문제
------------------------------------------------------------
초기 테스트에서는 다음 문제가 반복되었다.

- OCR이 말풍선 전체를 놓치면 번역/erase 자체가 시작되지 않음.
- OCR이 artwork를 text로 오인하면 잘못된 erase 후보로 전파될 수 있음.
- Vision OCR 보정이 주변 대사를 빌려오거나 지나치게 확장할 수 있음.
- OCR line geometry와 erase geometry가 너무 밀접하게 결합되어 있었음.
- 하나의 실제 말풍선이 여러 OCR fragment/unit로 쪼개지거나,
  반대로 먼 문장들이 하나의 unit으로 합쳐지는 문제가 있었음.

리팩터링 방향
------------------------------------------------------------
파이프라인을 다음 evidence stage로 분리하는 방향을 확정했다.

Page analysis
-> page-wide container candidate detection
-> candidate validation
-> global/focus/container OCR
-> OCR observation merge
-> line validation
-> line-to-container ownership
-> reading order
-> translation unit creation
-> Vision OCR review
-> final translation
-> erase decision
-> layout planning
-> erase/inpaint
-> typeset
-> post-render validation

핵심 구조 변화
------------------------------------------------------------
- OCR observation을 pass별로 보존.
- page-wide container candidate를 OCR seed 없이 생성.
- container 1x/2x OCR을 도입하고 cross-scale agreement를 evidence로 사용.
- line ownership을 explicit하게 기록.
- one canonical line = one active owner 원칙 도입.
- ambiguous/weak ownership은 강제로 배정하지 않고 orphan 또는 held 상태로 둠.
- container-first grouping과 legacy fallback grouping을 분리.
- Vision correction은 번역 보조이지만 erase 권한을 직접 주지 않음.
- 실제 detector/safe-mask 근거가 없는 fallback rectangle은 erase 권한을 주지 않음.
- short/low-confidence OCR fragment가 Vision에서 긴 문장으로 과도하게 확장되는 경우 차단.
- lossless WebP 기반 결과/디버그 출력 구조를 정리.

관련 설계 문서:
- docs/PIPELINE_REFACTOR.md
- ARCHITECTURE.md
- docs/ARCHITECTURE_2.md


============================================================
0019 - Atomic erase + typeset commit
============================================================

문제
------------------------------------------------------------
erase는 성공했지만 layout/typeset이 실패하거나,
반대로 검수 실패가 뒤늦게 발생하면 말풍선이 빈 채로 남는 문제가 있었다.

변경
------------------------------------------------------------
- TextBubble이 source-text pixel과 erase review를 소유.
- 이미 연결된 parent Bubble이 layout/font fitting을 소유.
- layout validation을 erase보다 먼저 수행.
- erase review까지 통과한 unit만 committed cleaned image에 복사.
- binding/layout/mask/residual review 중 하나라도 실패하면
  해당 unit은 원본 픽셀을 유지하고 한국어 overlay도 하지 않음.
- v2_05_committed_cleaned.webp를 원본 + 승인된 TextBubble edit만으로 구성.
- v2_commit_audit.json에 각 requested unit을
  translated 또는 original_preserved:* 상태로 기록.

핵심 invariant
------------------------------------------------------------
EraseCommittedUnits == TypesetCommittedUnits

추가 안정화
------------------------------------------------------------
OCR line consolidation에서 confidence가 비슷하면 더 완전한 line을 우선하도록 하여
THE MIDDLE 같은 fragment가
YOU'RE STILL IN THE MIDDLE 같은 완전 문장을 덮어쓰지 않도록 함.


============================================================
0020 - TextFree / colored glyph / 4-stage audit
============================================================

관찰된 문제
------------------------------------------------------------
1. RT-DETR TextFree detection이 region analysis에는 있지만 V2 immutable target에는 빠짐.
2. 빨간 NONONO 같은 colored lettering이 grayscale residual review를 통과해 남을 수 있음.
3. 완료검토가 ORIGINAL | FINAL | DIFF뿐이라 erase 단계 문제와 typeset 문제를 구분하기 어려움.

변경
------------------------------------------------------------
- V2DetectionSnapshot에 TextBubble + TextFree를 모두 포함.
- TextBubble은 parent Bubble을 layout anchor로 유지.
- TextFree는 parent Bubble을 새로 만들지 않고 detector rect 자체를 conservative anchor로 사용.
- colored glyph rescue에서 chroma-contrast component를 제한적으로 추가.
- final audit를 가능한 경우 4단계로 변경:
  1 ORIGINAL
  2 ERASE COMMIT
  3 FINAL
  4 DIFF
- audit summary에 preserve reason을 기록.

의도
------------------------------------------------------------
erase와 typeset failure를 분리해 볼 수 있게 하고,
color text를 grayscale-only assumption에서 벗어나 처리.


============================================================
0021 - Chroma residual false-positive 회귀 수정
============================================================

문제
------------------------------------------------------------
0020의 chroma rescue를 cleaned image residual review에도 그대로 재사용하면서,
Telea가 만든 색 변화까지 source glyph 잔재로 오판하는 문제가 생김.

변경
------------------------------------------------------------
- chroma rescue는 initial erase mask에만 사용.
- post-inpaint residual review에서는 chroma detector를 끄고
  원래 glyph 주변의 일반 high-contrast 구조만 검토.
- ordinary dialogue가 colored cleanup 때문에 회귀하지 않도록 범위를 분리.

추가
------------------------------------------------------------
짧고 넓은 caption/sign box의 font-scale/layout을 제한적으로 보정.


============================================================
0022 - Targeted tuning
============================================================

변경
------------------------------------------------------------
- 첫 pass가 허용 density 안에 있으면 불필요한 retry를 하지 않음.
- genuinely flat한 TextFree dark caption은 local panel color로 직접 채우는 path 허용.
- stylized_graphic heuristic을 좁혀 BRUCE!, BINGO!, LET ME OUTTT! 같은
  짧은 graphic text는 보존하고 긴 uppercase dialogue는 번역 가능하게 유지.
- short wide caption을 parent geometry 안에서 더 자연스럽게 사용.

당시 23-page 결과
------------------------------------------------------------
committed translations: 149/190 -> 158/190
erase-review failures: 14 -> 6


============================================================
0023 - Best-pass erase + translation safety
============================================================

문제
------------------------------------------------------------
retry가 항상 first pass보다 낫지 않은데도 retry 결과를 최종으로 쓰는 경우가 있었음.

변경
------------------------------------------------------------
- retry는 optional candidate로 취급.
- first-pass와 retry residual score를 target별 비교.
- retry가 나쁘면 해당 footprint만 first-pass cleaned image에서 복구.
- SelectedResidualPixels / SelectedPass audit 추가.

번역 safety gate
------------------------------------------------------------
- punctuation/noise-only corrected text는 translator로 보내지 않음.
- placeholder/template response reject + 1회 repair.
- NOT/CAN'T/DON'T/WON'T/NEVER 같은 영어 negation이 한국어 negation cue를 잃으면 retry.
- final result와 Vision draft 모두 실패하면 원본 보존.


============================================================
0024 - Residual review correction
============================================================

관찰된 문제
------------------------------------------------------------
실제 cleaned image는 이미 비어 있는데,
reviewer가 inpaint texture나 주변 contrast를 다시 text처럼 잡아서
atomic commit이 영어 원문을 되살리는 false-positive가 발생.

변경
------------------------------------------------------------
- 7x7 local review halo 복원.
- raw residual과 effective residual 분리.
- original erase mask 안의 재-segmented pixel은 surviving source text로 보지 않음.
- retry는 immutable glyph mask 밖의 suspicious residual을 중심으로 판단.
- raw residual은 sanity bound로 유지.
- best-pass selection 유지.
- raw/effective residual before/after retry를 audit에 기록.


============================================================
0025 - Source-glyph persistence review
============================================================

문제
------------------------------------------------------------
bubble border, panel line, inpaint texture가 review halo 안에 있다는 이유만으로
erase 실패로 판정되는 경우가 남음.

변경
------------------------------------------------------------
- undilated source-glyph core를 별도 추출.
- residual component가 source-glyph core와 실제로 연결되는지 확인.
- original과 cleaned pixel을 core-linked location에서 비교.
- 주변 구조는 diagnostic noise로 보고 reject 근거에서 제외.
- core-mask size / overlap / persistence ratio / ReviewReason audit 추가.

주의
------------------------------------------------------------
이 persistence reviewer를 primary로 쓰면 기존 안정 페이지 회귀가 생길 가능성이 있었음.


============================================================
0026 - Primary gate + secondary rescue
============================================================

결정
------------------------------------------------------------
0024 reviewer를 primary gate로 유지하고,
0025 glyph-persistence reviewer는 primary가 실패한 경우에만 secondary rescue로 사용.

동작
------------------------------------------------------------
- primary pass -> 즉시 commit 가능.
- primary fail -> secondary glyph persistence 실행.
- secondary clean -> rescue.
- 둘 다 fail -> 원본 보존.
- secondary reviewer는 primary-pass unit을 뒤집지 못함.

추가 audit
------------------------------------------------------------
PrimaryReview
SecondaryReview
RescuedBySecondary
ReviewReason

추가 debug
------------------------------------------------------------
v2_01b_glyph_core.webp
v2_03a_core_linked_residual.webp
v2_03b_persistent_core.webp


============================================================
0027 - Cleaned checkpoint audit
============================================================

목적
------------------------------------------------------------
erase와 typeset 사이에
"detector-owned erase target이 실제로 모두 비워졌는가?"
를 독립적으로 확인하는 checkpoint가 필요했음.

변경
------------------------------------------------------------
- immutable detector identity 기준 checkpoint 생성.
- 같은 parent Bubble의 TextBubble은 하나의 Bubble checkpoint로 묶음.
- TextFree는 독립 checkpoint.
- ID-set equality로 target/verified set 비교.
- diagnostic-only로 도입하고 0026 commit 정책은 아직 바꾸지 않음.

추가 debug
------------------------------------------------------------
v2_04b_cleaned_checkpoint.webp


============================================================
0028 - Independent CleanedStateVerifier
============================================================

핵심 문제
------------------------------------------------------------
pixel reviewer가 false-negative/false-positive를 내며
이미 잘 지운 영어를 원본 복구로 다시 살리는 문제가 남아 있었음.

변경
------------------------------------------------------------
1. 원본 RT-DETR Bubble/TextBubble/TextFree ID + geometry freeze.
2. selected immutable target erase.
3. v2_04_cleaned_final.webp 저장.
4. cleaned image에 RT-DETR을 다시 실행.
5. cleaned TextBubble/TextFree detection과 original selected TextRegion 비교.
6. cleaned text detection이 없으면 EMPTY_OK.
7. translation unit이 가진 모든 immutable TextRegionId가 EMPTY_OK일 때만 commit.
8. legacy pixel reviewer 결과는 diagnostic evidence로 유지.

중요
------------------------------------------------------------
cleaned RT-DETR은 evidence만 제공한다.
새 target을 만들거나 기존 erase geometry를 움직이지 않는다.

추가 파일
------------------------------------------------------------
v2_04a_legacy_reviewer_checkpoint.webp
v2_04b_cleaned_checkpoint.webp
v2_cleaned_state_audit.json

당시 핵심 결과
------------------------------------------------------------
기존 문제 페이지:
- core_caption_speech_mixed_001
- core_mixed_dialogue_caption_sfx_001

두 페이지 모두 rescue 성공.

23-page 기준:
Requested 184
translated/commit 162
preserved 22
erase/typeset 162
missing 0
erase fail 0
cleaned text redetected 0

남은 blind spot
------------------------------------------------------------
RT-DETR이 readable text로 재검출하지 못할 정도로 글자가 망가지면,
사람 눈에는 얼룩/잔재가 보여도 EMPTY_OK가 될 수 있음.


============================================================
0029 - Glyph-topology-preserving erase mask
============================================================

관찰된 문제
------------------------------------------------------------
NONONO 같은 빨간 fill + 검은 outline에서 external contour를 solid fill하던 방식이
O/P/R 내부 hole과 글자 사이 negative space까지 erase pixel로 먹음.
이후 Telea가 검정/빨강을 잘못 끌어와 blotch를 생성.

변경
------------------------------------------------------------
- contour는 component selection에만 사용.
- 실제 threshold foreground pixel만 output mask로 복사.
- glyph hole / inter-letter negative space 보존.
- color rescue는 실제 chroma seed + 가까운 high-contrast outline만 추가.
- final 3x3 dilation은 topology-correct foreground mask에 적용.

추가 debug
------------------------------------------------------------
v2_01a_grayscale_mask.webp
v2_01c_color_rescue_mask.webp
v2_01_text_mask.webp

추가 audit
------------------------------------------------------------
GrayscaleMaskPixels
ColorRescueMaskPixels

결과
------------------------------------------------------------
23-page 전체 큰 회귀는 없었음.
하지만 mask가 좋아져도 Telea가 주변 red/black ink를 source material로 사용해
colored blotch를 만드는 문제는 남음.


============================================================
0030 - Background reconstruction strategy 분리
============================================================

핵심 판단
------------------------------------------------------------
Text Removal =
Foreground segmentation
+
Background reconstruction

즉 "어떤 pixel이 글자인가"와
"그 pixel 뒤에 무엇을 복원해야 하는가"를 분리해야 함.

변경
------------------------------------------------------------
TextRegionId별 reconstruction strategy 도입.

FLAT_FILL
- flat Bubble/Caption의 dominant background color를 추정.
- accepted glyph mask pixel을 해당 background color로 직접 채움.

TELEA
- artwork / gradient / texture / ambiguous background에 사용.

추가 debug
------------------------------------------------------------
v2_02a_reconstruction_strategy.webp
v2_background_audit.json

추가 audit
------------------------------------------------------------
Strategy
MaskPixels
SampleCount
Background B/G/R
DominantMatchRatio
P75ColorDistance
P90ColorDistance
Reason
PostReconstructionP85

NONONO RG012 결과
------------------------------------------------------------
0030에서는 TELEA로 분류.
Reason = background_not_dominant 계열.

원인
------------------------------------------------------------
TextBounds 내부 non-mask pixel median이
mask 바깥의 red/black anti-alias 및 outline에 오염되어
실제 밝은 말풍선 background를 제대로 추정하지 못함.


============================================================
0031 - Purified background candidate clustering
============================================================

목표
------------------------------------------------------------
background sample에서 glyph-adjacent contamination을 제거하고
실제 dominant flat background를 선택.

변경
------------------------------------------------------------
1. glyph mask adaptive dilation으로 주변 오염 sample 제외.
2. 남은 pixel을 coarse color bin으로 분류.
3. 강한 cluster 후보 생성.
4. purified samples 전체에서 strongest compact cluster 선택.
5. 4x4 grid spatial coverage 검사.
6. flat 판정에 dominant ratio + P90 distance + spatial coverage 사용.

추가 audit
------------------------------------------------------------
SpatialCoverage
ExclusionRadius
Reason:
- dominant_purified_background_cluster
- background_cluster_weak
- background_cluster_variance
- background_cluster_not_spatial

NONONO RG012 결과
------------------------------------------------------------
Strategy = FLAT_FILL
Background approx B246 G242 R241
DominantMatchRatio approx 0.8845
SpatialCoverage approx 0.625

즉 "뒤는 밝은 flat balloon" 판정 자체는 성공.

새로 드러난 실패
------------------------------------------------------------
첫 복원 뒤 colored glyph 일부가 남음.
retry 단계가 모든 retryMask를 page-level Telea로 보내면서
첫 pass에서 선택한 FLAT_FILL strategy를 버림.

실제 흐름:
FLAT_FILL
-> residual
-> retry
-> Telea
-> 남은 red/black ink를 source로 사용
-> colored blotch

또한 당시 BackgroundQualityCheck는
"우리가 채운 mask pixel이 background color와 비슷한가"만 봐서
mask 밖에 남은 colored glyph는 놓칠 수 있었음.


============================================================
0032 - Strategy-stable retry + flat residual quality gate
============================================================

목표
------------------------------------------------------------
FLAT_FILL target은 retry에서도 FLAT_FILL.
TELEA target은 retry에서도 TELEA.

변경
------------------------------------------------------------
- page-level retryMask 외에 flatRetryMask / teleaRetryMask 분리.
- backgroundAudits를 TextRegionId dictionary로 유지.
- target별 retryTargetMask 보존.
- FlatAccepted=true이면 저장된 Background RGB로 ApplyFlatFill retry.
- FLAT_FILL retry pixel은 Telea retry mask에서 제외.
- TELEA target만 InpaintOnlyMaskedPixels 사용.
- detector box overlap이 있어도 FLAT_FILL pixel ownership 우선.

추가 audit
------------------------------------------------------------
RetryReconstruction:
- FlatFillRetryTargets
- FlatFillRetryMaskPixels
- TeleaRetryTargets
- TeleaRetryMaskPixels

target:
RetryStrategy =
- FLAT_FILL
- TELEA
- TELEA_FALLBACK
- not_run

추가 quality gate
------------------------------------------------------------
FLAT_FILL retry 이후 original glyph 주변 expanded review ring에서
estimated background와 멀리 떨어진 strongly chromatic pixel을 검사.
neutral black/gray bubble outline은 colored residual로 오인하지 않도록 분리.

실제 0032 hard_long_speech_001 / RG012
------------------------------------------------------------
RetryStrategy = FLAT_FILL
FlatFillRetryTargets = 1
TeleaRetryTargets = 0
ResidualBeforeRetry approx 13073
ResidualAfterRetry approx 2001

PostRetryChromaticReviewPixels = 22676
PostRetryChromaticOutlierPixels = 3822
OutlierRatio approx 0.168548 (16.85%)
BackgroundQualityPass = false

따라서 최종 commit은
original_preserved:not_committed

의미
------------------------------------------------------------
0031의 "retry가 Telea로 돌아가 빨간 덩어리를 생성"하는 문제는 해결.
하지만 NONONO 자체를 완전히 깨끗하게 지우는 데는 아직 실패.
대신 품질 실패를 감지하고 원본을 안전하게 보존하는 데 성공.


============================================================
0033 - Flat residual cleanup
============================================================

목표
------------------------------------------------------------
0032가 이미 정확히 찾아낸 flat-background chromatic residual을
단순 fail signal로만 쓰지 않고 실제 constrained cleanup mask로 활용.

설계 원칙
------------------------------------------------------------
- FLAT_FILL target에만 적용.
- TELEA target에는 적용하지 않음.
- quality gate를 통과한 정상 flat target은 건드리지 않음.
- cleanup geometry는 immutable TextBounds와 original glyph-adjacent review zone 안으로 제한.
- 특정 색(red)이나 특정 문자열(NONONO)에 하드코딩하지 않음.
- colored residual을 seed로 사용.
- seed와 직접 연결된 neutral dark outline은 같이 cleanup 가능.
- 떨어져 있는 neutral bubble/artwork line은 보존.
- cleanup도 Telea를 쓰지 않고 저장된 flat Background RGB로 직접 복원.
- cleanup 후 같은 BackgroundQualityCheck를 다시 실행.
- 그래도 fail이면 기존처럼 original preserve.

코드 변경
------------------------------------------------------------
ErasePipelineV2.cs

추가 단계:
FLAT_FILL
-> retry FLAT_FILL
-> chromatic residual 검사
-> linked flat residual cleanup
-> same Background RGB로 repaint
-> background quality 재검사
-> commit 또는 preserve

추가 public/helper logic
------------------------------------------------------------
IsFlatBackgroundQualityFailure(...)
BuildFlatChromaticOutlierMask(...)
BuildFlatResidualCleanupMask(...)

cleanup candidate는 estimated background에서 충분히 멀리 떨어진 pixel만 허용하고,
colored seed에서 제한된 거리만 region-growing 하도록 구성.
이 방식으로 colored fill에 붙은 검정 outline은 포함할 수 있지만
분리된 bubble border는 잡지 않도록 함.

추가 debug
------------------------------------------------------------
v2_03c_flat_residual_outlier.webp
- colored residual seed 표시

v2_03d_flat_cleanup_mask.webp
- 실제 마지막 FLAT_FILL cleanup 영역 표시

추가 audit
------------------------------------------------------------
FlatResidualCleanup:
- AppliedTargets
- CleanupMaskPixels
- OutlierDebugFile
- CleanupMaskDebugFile

각 BackgroundReconstruction item:
- 기존 PostRetryChromaticReviewPixels
- 기존 PostRetryChromaticOutlierPixels
- 기존 PostRetryChromaticOutlierRatio
- FlatResidualCleanupApplied
- FlatResidualCleanupMaskPixels
- PostCleanupChromaticReviewPixels
- PostCleanupChromaticOutlierPixels
- PostCleanupChromaticOutlierRatio
- BackgroundQualityPass

회귀 테스트
------------------------------------------------------------
PipelineChecks에 다음 케이스 추가.

- colored residual은 cleanup seed가 되어야 함.
- seed에 붙은 neutral black outline은 cleanup mask에 포함되어야 함.
- 같은 review zone에 있어도 flat background로 분리된 unrelated neutral line은 보존되어야 함.
- cleanup pixel은 저장된 Background RGB로 정확히 repaint되어야 함.

관련 커밋
------------------------------------------------------------
52fec6591cb7b87c820dcdcac5289aea63626455
V2 0033: clean linked residual ink on flat backgrounds

48745f7c41c57db7b649d29e293d62566d026977
Tests: lock 0033 flat residual cleanup

실제 0033 hard_long_speech_001 / RG012 결과
------------------------------------------------------------
Strategy = FLAT_FILL
RetryStrategy = FLAT_FILL
TeleaRetryTargets = 0

cleanup 전:
PostRetryChromaticReviewPixels = 22676
PostRetryChromaticOutlierPixels = 3822
PostRetryChromaticOutlierRatio = 0.16854824484035985

cleanup:
FlatResidualCleanupApplied = true
FlatResidualCleanupMaskPixels = 3822

cleanup 후:
PostCleanupChromaticReviewPixels = 22676
PostCleanupChromaticOutlierPixels = 0
PostCleanupChromaticOutlierRatio = 0
BackgroundQualityPass = true

최종 commit:
RequestedUnits = 8
TranslatedUnits = 8
PreservedOriginalUnits = 0
EraseCommittedUnits = 8
TypesetCommittedUnits = 8
MissingUnits = 0
DuplicateUnits = 0

시각 결과:
- NONONO red fill 제거 성공.
- black outline 잔재 제거 성공.
- 0031에서 보이던 red blotch 없음.
- 0032처럼 원본으로 rollback하지 않음.
- Korean typeset 정상 commit.

0033 전체 23-page regression
------------------------------------------------------------
Requested = 184
Translated/Committed = 162
PreservedOriginal = 22
EraseCommitted = 162
TypesetCommitted = 162
Missing = 0
Duplicate = 0
BackgroundQualityFailures = 0
EraseReviewFailedUnits = 0
CleanedTextRedetectedUnits = 0

retry:
총 retry target = 4
FLAT_FILL retry = 4
TELEA retry = 0

0033 FlatResidualCleanup:
AppliedTargets = 1
CleanupMaskPixels = 3822

중요 해석
------------------------------------------------------------
cleanup이 전체 페이지에 무분별하게 퍼지지 않았다.
23-page regression에서 실제 cleanup이 발동한 것은
hard_long_speech_001 / RG012 한 target뿐이었다.

따라서 현재 테스트 범위에서는
NONONO 한 케이스를 해결하기 위해 일반 대사/캡션 erase 안정성이 회귀한 징후가 없음.

0033은 현재 이미지 삭제/flat-background reconstruction 기준 baseline으로 사용한다.


============================================================
0033 이후 실전 챕터 테스트
============================================================

다음 검증 단계
------------------------------------------------------------
이제 개별 regression page가 아니라
지금까지 테스트에 사용한 Batman 한 챕터 전체 28장을 0033으로 번역하여
실제 연속 페이지에서 pipeline 편차를 확인한다.

이번 28-page 챕터에서 분류할 문제
------------------------------------------------------------
1. Detection / OCR omission
2. Canonical source-text loss
3. Erase / background reconstruction
4. Cleaned-state verification
5. Layout / typesetting
6. Translation/context quality
7. Stylized/SFX preservation policy

현재 처음 관찰된 실전 이슈
------------------------------------------------------------
page032 완료검토에서 작은 녹색/회색 대사
"AM I RIGHT?"
가 ORIGINAL에서 존재하지만 ERASE COMMIT에서도 그대로 남고
FINAL에서도 번역되지 않은 사례가 발견됨.

같은 주변의 "BATS?"는 erase + 번역되어 "박쥐?"로 정상 commit.

현재 이미지 증거만으로 확정할 수 있는 것:
- "AM I RIGHT?"는 erase 단계에서 지워진 뒤 복구된 것이 아님.
- ERASE COMMIT 단계부터 원본 그대로 존재하므로
  erase target/translation binding 이전 upstream에서 제외된 것으로 보임.

아직 확정하지 않은 것:
- RT-DETR TextBubble 자체 미검출인지
- OCR observation 누락인지
- OCR은 있었지만 confidence/ownership에서 탈락했는지
- translation unit은 있었지만 TextRegion binding이 실패했는지

다음 조사 시 확인할 파일:
- page032.translated.00_page_candidates.json
- page032.translated.00_ocr_observations.json
- page032.v2_commit_audit.json
- page032.v2_erase_audit.json
- 필요 시 container OCR validation / line ownership / final audit

중요:
"AM I RIGHT?"는 일반 말풍선 대사로 보이므로
stylized/SFX 보존 정책으로 숨길 대상이 아니다.
원인은 evidence chain을 확인한 뒤 일반화 가능한 방식으로 수정해야 한다.


============================================================
앞으로 PATCH_NOTES.txt 업데이트 규칙
============================================================

새 버전마다 아래 형식을 추가한다.

------------------------------------------------------------
00XX - 버전 제목

목표
- 왜 이 버전이 필요한지.

관찰된 문제
- 실제 이미지/audit에서 무엇이 실패했는지.
- 재현 page / RegionId / 핵심 수치.

원인
- 코드/파이프라인상 실제 원인.
- 추정일 경우 "추정"이라고 명시.

변경
- 수정한 파일.
- 바꾼 algorithm / threshold / stage / invariant.
- 일부러 바꾸지 않은 부분.

Debug / Audit
- 새 파일명.
- 새 JSON field.
- 기존 field 의미 변경 여부.

Regression tests
- 어떤 실패를 고정했는지.
- 정상 케이스가 어떻게 보존되는지.

실제 테스트 결과
- single-page 결과.
- full regression 결과.
- before/after 수치.

남은 문제
- 아직 해결하지 않은 것.
- 다음 버전 후보.

관련 커밋
- SHA
- commit message
------------------------------------------------------------

운영 원칙
------------------------------------------------------------
- 버전별 TXT를 여러 개 만들지 않는다.
- 이 PATCH_NOTES.txt 한 파일에 계속 누적한다.
- 새 채팅에서는 최신 section과 "남은 문제"를 먼저 읽는다.
- 테스트 결과가 GitHub에 push되면 코드 변경 전 먼저 audit을 읽고 원인을 확정한다.
- 단순 증상 패치보다 전체 pipeline invariant를 우선한다.
- 특정 단어/특정 페이지 하드코딩은 피한다.
- 일반 대사 안정성을 깨는 수정은 허용하지 않는다.


============================================================
0034 - Detector-owned Commit Units + Target Coverage
============================================================

배경 / 실전 29-page 챕터 감사
------------------------------------------------------------
0033_2에서 Batman page004~page032 총 29장을 연속 처리한 결과,
erase/background reconstruction 자체보다 앞단의 ownership/unit 구조에서
여러 종류의 실제 누락과 오병합이 확인되었다.

0033_2 전체 audit:
Requested = 261
Translated = 223
Preserved = 38
EraseCommitted = 223
TypesetCommitted = 223
Missing = 0
CleanedTextRedetected = 3
Unbound = 8
DuplicateSuppressed = 4
OcrNoise = 20
OtherPreserved = 2

중요:
Missing=0이지만 실제 결과에는 영어가 남은 말풍선이 존재했다.
기존 Missing은 이미 requested translation unit까지 올라온 항목만 보므로
detector target -> OCR -> unit -> binding -> final commit 전체 coverage를
나타내지 못했다.


관찰된 대표 회귀
------------------------------------------------------------
1. page012
상단 작은 말풍선:
  Nononono... no...
옆 free-standing graphic:
  LET ME OUTTT!

RT-DETR은 정상적으로 분리:
RG002 = TextBubble (작은 말풍선)
RG003 = TextFree (빨간 graphic text)

하지만 broad orphan OCR unit:
  "RU LETT ME OUTTT!"
를 Vision이
  "Nononono... no... / LET ME OUTTT!"
로 확장했고,
기존 V2 geometry fallback이 RG002 + RG003을 동시에 잡아
하나의 commit unit으로 만들었다.

이 combined unit이 cleaned-state fail하면서 두 owner가 같이 rollback.
별도로 정확히 읽힌 "Nononono.. no..." unit은
container_duplicate로 억제되어 정상 말풍선까지 번역되지 않았다.


2. page017
2번째 컷의 서로 다른 두 말풍선:
  "COME ON, COME ON."
  "IT HAS TO BE HERE SOMEWHERE, BRUCE..."

OcrContainerUnitBuilder 이후 ConsolidateRegionOwnedUnits가
같은 CandidateId라는 이유만으로 두 OCR line stack을 하나로 합쳤다.

결과:
- 두 말풍선 원문이 하나의 Vision/translation unit이 됨.
- 번역문 전체가 한쪽 작은 말풍선에 들어감.
- 다른 물리 말풍선은 원문이 남음.


3. page021
중간:
  "I DON'T KNOW ANYMORE. I DON'T--"

RT-DETR / OCR / Baberu 모두 evidence 존재.
Vision result:
  Type = dialogue
  Render = false
  Translation = ""

즉 detector/OCR 누락이 아니라,
확실한 TextBubble dialogue 하나가 Vision의 Render=false 한 번으로
최종 번역 경로에서 사라졌다.


4. page028
"So first..." 주변 두 물리 말풍선이 하나의 번역/조판 unit으로 섞여
두 문장의 번역이 위쪽 말풍선에 몰리고 아래쪽 영어가 남음.
page017과 같은 CandidateId 중심 consolidation 계열로 판단.

또한 왼쪽 일부 일반 대사는 별도 dropout이 관찰됨.


5. page032
"AM I RIGHT?"

두 후보가 같은 RG013을 두고 경쟁:
- 약한 OCR "1 La" -> Vision expansion "AM I RIGHT?" -> semantic unsafe
- 정확한 OCR "AMI RIGHT?" + Baberu agree -> 정상 후보

기존 flow는 동일 물리 container 후보를 planning 전에 먼저 억제했기 때문에
약한 후보가 먼저 선택된 뒤 실패하면 정상 후보를 다시 시도할 수 없었다.


6. page014 NONONO
0033 단일 regression image와 챕터 page014는 시각적으로 같은 페이지지만
디지털 입력 크기가 달랐다.

기존 regression: 1331 x 2048
chapter page014: 1341 x 2062

chapter page014에서는 OCR은 NONONO를 읽었지만
RT-DETR Bubble/TextBubble detector target 자체가 생성되지 않아
RegionId=null, original_preserved:unbound가 됨.

이 문제는 0034 ownership 수정과 별개의 detector scale/resize 민감성 문제로 남긴다.


0034 핵심 원칙
------------------------------------------------------------
Translation context와 physical commit ownership을 분리한다.

문맥은 여러 말풍선을 함께 참조할 수 있지만,
erase / layout / review / rollback은 immutable detector owner별로 독립해야 한다.

Hard rule:
- 서로 다른 RT-DETR TextRegion owner는 하나의 physical commit unit이 될 수 없음.
- TextBubble과 TextFree는 geometry fallback에서 같이 묶지 않음.
- 서로 다른 parent Bubble은 하나의 commit unit으로 묶지 않음.
- 같은 candidate rectangle만으로 두 TextRegion을 합치지 않음.
- rejected candidate가 valid duplicate candidate를 미리 제거하지 못함.


변경 1 - detector TextRegion 기준 consolidation
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/OcrPipelineService.cs

기존:
ConsolidateRegionOwnedUnits는 CandidateId만 같으면 unit들을 하나로 합침.

문제:
한 candidate가 인접한 여러 text region을 품으면 서로 다른 물리 말풍선까지
하나의 Vision input/translation unit으로 병합.

0034:
ConsolidateRegionOwnedUnits에 pageCandidates + PageRegion evidence를 전달.
각 block의 best immutable TextBubble RegionId를 먼저 확인한 뒤:

CandidateId + DetectorTextRegionId

가 모두 같은 경우에만 consolidation 허용.

detector text-region ownership을 확정하지 못한 candidate-owned unit은
합치지 않고 원래 unit으로 격리 유지.

목표:
page017 / page028의 multi-balloon translation collapse 차단.

관련 커밋:
ae5be5393212557a891ab99e573c49a86bef17ff
V2 0034: keep detector text regions as commit-unit boundaries


변경 2 - geometry fallback은 한 detector ownership domain만 선택
------------------------------------------------------------
파일:
src/LocalMangaTranslator/PipelineV2/Erase/V2EraseSelector.cs

기존:
geometry fallback 후보를 score 순으로 .Take(2) 하여
인접 TextBubble + TextFree 또는 서로 다른 bubble target이
하나의 V2 binding에 들어갈 수 있었음.

0034:
- exact TextRegion link 우선.
- BubbleRegionId link 우선.
- 마지막 geometry fallback에서는 best ownership domain 하나만 선택.
- best가 TextFree이면 그 TextFree 하나만.
- best가 TextBubble이면 같은 parent Bubble의 TextBubble만 허용.
- TextBubble + TextFree cross-domain binding 금지.
- 서로 다른 parent Bubble cross-domain binding 금지.

Stylized preserve의 경우 translation region id뿐 아니라
immutable target id 기준 PreservationTargetReasons도 기록.

목표:
page012 RG002 + RG003 오병합 차단.

관련 커밋:
461c12cea628046a3065c0652cddf13e6b0d7c02
V2 0034: constrain fallback binding to one detector owner


변경 3 - V2 duplicate candidate arbitration을 safety check 이후로 이동
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/RenderPipelineService.cs

기존:
같은 physical container라고 판단된 후보는 BuildRenderPlans 시작 전에
ContainerKeepScore 순서로 하나만 남김.

문제:
약한 후보가 먼저 선택되고 나중에 semantic/layout fail하면,
이미 제거된 정상 OCR 후보는 다시 시도할 수 없었음.

0034:
Pipeline V2에서는 동일 container 후보를 planning 전에 제거하지 않음.
모든 candidate에 대해 먼저:
- semantic safety
- layout feasibility
를 계산.

V2에서는 canonical OCR line conflict를 erase ownership으로 사용하지 않음.
V2의 erase ownership authority는 immutable RT-DETR TextRegion.

승인 가능한 후보들 사이에서만 최종 duplicate arbitration 수행.

V2CandidateKeepScore 우선 근거:
- exact RegionTextRegion evidence
- RegionId structural link
- Secondary OCR agreement
- primary OCR average confidence
- existing completeness score
- suspicious Vision expansion에는 큰 penalty

목표:
page032에서
"1 La" expansion 후보가 실패해도
"AMI RIGHT?" + Baberu agree 후보가 살아서 commit 가능하도록 함.

관련 커밋:
cb7e286bce2577617f831c536c7f29ae30723781
V2 0034: arbitrate duplicate candidates after safety checks


변경 4 - detector target coverage audit
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/RenderPipelineService.cs

신규 debug JSON:
<page>.v2_target_coverage_audit.json

모든 V2DetectionSnapshot.TextTargets를 기준으로 최종 상태를 기록.

대표 Status:
- translated_committed
- preserved_policy:<reason>
- preserved_render_false:<type>
- preserved_plan:<reason>
- preserved_cleaned_state_or_background_quality
- unresolved_bound_without_plan
- unresolved_detected_no_translation_unit
- unresolved_detected_unbound_translation

summary:
DetectedTextTargets
CommittedTargets
PreservedTargets
UnresolvedTargets
CoverageComplete

목적:
화면에는 영어가 남았는데 기존 commit audit의 MissingUnits=0으로 보이는
blind spot을 제거.

관련 커밋:
65ce5ceacdc96eab658708071409747501db4634
V2 0034: add detector target coverage audit

42bbff4f48b43830f6ca8b1dabbf596f79b473c7
Fix 0034 target coverage for region id zero


변경 5 - 확실한 detector-owned dialogue의 Vision Render=false 복구
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/PagePipelineService.cs

조건:
- Vision Render=false
- immutable RegionTextRegion이 TextBubble
- RegionContainer 존재
- Type = dialogue / thought / caption
- corrected/source가 meaningful text
- Baberu/secondary OCR evidence가 있거나
  primary OCR 평균 confidence >= 0.80

위 조건이면 Render=true로 재활성화한 뒤
기존 isolated final TranslationRefinementService에 보내어 번역을 다시 시도.

중요:
이 recovery는 곧바로 erase 권한을 주지 않는다.
이후에도 V2 selector / layout / erase / cleaned-state gate를 모두 통과해야 commit.

목표:
page021
"I DON'T KNOW ANYMORE. I DON'T--"
처럼 detector/OCR evidence가 충분한 일반 대사가
Vision의 단일 Render=false 판단만으로 사라지지 않게 함.

관련 커밋:
a83810ee4775e5134430d9a404b287250bc73c2d
V2 0034: recover detector-owned dialogue from Vision render drops

86be56035e10732a96e4f4aeccc276892a676b70
Refactor 0034 detector-owned render recovery into testable policy


변경 6 - 동일 입력에 대한 LLM 결정성 고정
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/VisionTranslationService.cs
src/LocalMangaTranslator/Services/TranslationRefinementService.cs

Vision review:
temperature = 0.0
seed = 34034 + recoveryAttempt

Final translation:
temperature = 0.0
seed = 34034

목적:
동일 image bytes + 동일 model + 동일 prompt일 때
Type / Render / corrected text / translation 결과가 sampling noise로 흔들리는 것을 최소화.

주의:
page014 NONONO의 이번 차이는 실제 input resolution이 달랐으므로
그 케이스 자체는 LLM nondeterminism으로 판정하지 않는다.

관련 커밋:
27d267af238c43be1d9269115e57292d5b63fc6f
V2 0034: make Vision review decisions deterministic

7b2456875347c1094d663771005f15eadd81d10a
V2 0034: make final translation deterministic


Regression tests
------------------------------------------------------------
1. broad orphan geometry가 TextBubble + TextFree를 동시에 덮어도
   V2 binding은 detector owner 하나만 선택해야 함.

2. free-standing stylized text preservation reason을
   translation id뿐 아니라 detector TextRegionId에도 기록.

3. detector-owned TextBubble dialogue가 Render=false라도
   secondary/strong OCR evidence가 있으면 recovery policy가 true.

4. 같은 조건이라도 Type=logo 같은 non-dialogue content는 강제로 복구하지 않음.

관련 커밋:
e8e164546b47529a4a1361f336fccebba5163ad5
Tests: lock 0034 detector-owner binding boundary

5bdb4e821dc227916b320c4d930e56bb627ea0ae
Tests: lock 0034 detector-owned render recovery


0034에서 의도적으로 건드리지 않은 것
------------------------------------------------------------
- 0033 FLAT_FILL / TELEA reconstruction algorithm
- 0033 chromatic residual cleanup
- CleanedStateVerifier detector logic
- RT-DETR model 자체 threshold / input preprocessing
- 번역 문장 스타일/말투 품질

이 버전의 목적은 erase 품질 재조정이 아니라
physical text ownership과 coverage의 안정화다.


0034 검증 우선 페이지
------------------------------------------------------------
전체 29장을 바로 다시 돌리기 전에 다음 페이지부터 재검증.

1. page012
기대:
- 작은 "Nononono... no..."와 TextFree "LET ME OUTTT!"가 하나 unit으로 묶이지 않음.
- 작은 말풍선은 독립 번역/commit 가능.
- TextFree는 별도 정책 적용.

2. page017
기대:
- "COME ON, COME ON."과
  "IT HAS TO BE HERE SOMEWHERE, BRUCE..."가 서로 다른 detector text owner로 유지.
- 한쪽 말풍선에 두 번역이 몰리지 않음.

3. page021
기대:
- "I DON'T KNOW ANYMORE. I DON'T--"가 detector-owned recovery로 final translator에 들어감.
- 정상 번역 후 독립 erase/typeset.

4. page028
기대:
- "SO FIRST..." 주변 두 말풍선이 각각 독립 commit unit.
- 아래쪽 원문이 남은 채 위쪽에 두 번역이 몰리는 현상 없어야 함.

5. page032
기대:
- weak "1 La" Vision expansion이 정상 "AMI RIGHT?" 후보를 제거하지 않음.
- "AM I RIGHT?" 정상 번역/commit.


남은 문제 / 다음 버전 후보
------------------------------------------------------------
1. page014 NONONO detector scale sensitivity
- chapter image에서 RT-DETR target 자체가 누락됨.
- OCR은 읽었지만 immutable detector owner가 없어 unbound.
- detector preprocessing / multi-scale inference / threshold robustness를 별도 검토.

2. cleaned_text_redetected
0033_2에서 page005 / page012 / page028 총 3개.
0034 ownership 수정 후 page012/page028 일부가 자연히 사라질 수 있으므로
0034 재검증 후 실제 잔여 케이스만 별도 분석.

3. target coverage 결과에서 unresolved target이 반복되면
detector-owned targeted OCR rescue를 다음 단계로 검토.



============================================================
0035 - Detector TextRegion Ownership Authority
============================================================

배경
------------------------------------------------------------
0034 실전 재검증은 page012 / page017 / page021 / page028 / page032 5장을 사용했다.

0034 결과에서 확인된 개선:
- page012의 작은 "Nononono... no..."가 TextFree "LET ME OUTTT!"와 분리되어 정상 번역/commit.
- page032의 정확한 "AMI RIGHT?" 후보가 weak "1 La" 후보에 의해 제거되지 않고 정상 commit.
- 신규 v2_target_coverage_audit.json이 기존 MissingUnits=0 blind spot을 실제로 드러냄.

그러나 다음 문제가 남았다.

page017:
- "COME ON, COME ON."은 정상 commit.
- 오른쪽 "IT HAS TO BE HERE SOMEWHERE, BRUCE..."는 원문 보존.
- commit reason = container_duplicate_after_v2_candidate_arbitration.
- target coverage:
  RG007 TextBubble <- translation Region 2, Region 4가 같이 몰림.
  RG008 TextBubble -> unresolved_detected_unbound_translation.
- translation Region 4 geometry는 실제 RG008 내부에 있는데 Source.RegionId가
  왼쪽 Bubble RG005로 잘못 기록됨.

page028:
- "I'LL TELL YOU IF YOU WANT..."는 정상 commit.
- 아래 "SO FIRST... A FAVOR? FOR AN OLD MAN?"은 원문 보존.
- target coverage:
  RG022 TextBubble <- Region 13, Region 16이 같이 몰림.
  실제 아래 RG024 TextBubble -> unresolved_detected_unbound_translation.
- Region 16 geometry는 RG024 내부인데 Source.RegionId가 앞 Bubble RG021로 잘못 기록됨.

page021:
- "I DON'T KNOW ANYMORE. I DON'T--"는 OCR A와 Baberu 모두 존재.
- detector Bubble RG013 / TextBubble RG014 존재.
- Vision result = Type dialogue, Render=false, Translation="".
- target coverage = preserved_render_false:dialogue.
- 0034 recovery는 runtime RegionTextRegion 직접 링크를 요구해서 이 unit을 살리지 못함.

page012 TextFree:
- 시각 결과는 원하는 대로 "LET ME OUTTT!" 원본 보존.
- 하지만 coverage에서는 RG003 TextFree가
  unresolved_detected_unbound_translation으로 표시.
- 사용자 정책은 SFX/effect/decorative TextFree는 번역/erase하지 않고 원본 보존이므로
  unresolved가 아니라 명시적 preserved policy가 되어야 함.


0035 핵심 원인
------------------------------------------------------------
0034는 "서로 다른 detector owner를 한 commit unit에 묶지 않는다"는
downstream boundary를 추가했지만,
upstream AttachRegionContainers가 여전히 legacy CandidateId를 먼저 신뢰했다.

기존 순서:
OCR unit
-> legacy CandidateId
-> candidate.RegionId
-> candidate 영역 안에서 TextBubble 탐색
-> RegionId / RegionTextRegion 부여

문제:
legacy page candidate가 인접한 두 말풍선을 넓게 포괄하거나
OCR ownership 단계에서 이웃 candidate를 선택하면,
실제 OCR block geometry가 다른 TextBubble 안에 있어도
candidate가 가리키는 Bubble 안에서만 TextBubble을 찾는다.

따라서 0034의 downstream duplicate arbitration에 도달하기 전에
translation unit 자체가 잘못된 physical owner를 이미 가진 상태가 됨.


0035 핵심 원칙
------------------------------------------------------------
Detector TextRegion이 ownership의 최종 authority다.

새 순서:
OCR block geometry
-> RT-DETR TextBubble 직접 매칭
-> TextBubble의 parent Bubble 결정
-> 그 parent Bubble에 대응하는 candidate를 역으로 선택
-> RegionId / RegionContainer / RegionTextRegion 갱신
-> UnitOwnership CandidateId와 LineOwnership도 detector owner 기준으로 교정

즉 ContainerCandidate는 더 이상 physical owner를 결정하지 않는다.
candidate는 detector owner가 정해진 뒤 mask/layout/OCR evidence를 제공하는
보조 자료로만 사용한다.


변경 1 - detector-first OCR ownership attachment
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/OcrPipelineService.cs

기존 AttachRegionContainers:
CandidateId -> candidate.RegionId -> candidate 내부 TextBubble 탐색.

0035:
1. OCR block geometry로 모든 RT-DETR TextBubble 중 best target을 직접 선택.
2. block coverage >= 0.45 또는 block center가 TextBubble 내부인 경우만 후보.
3. center containment -> block coverage -> detector score 순으로 선택.
4. 선택된 TextBubble의 parent Bubble을 detector geometry로 다시 결정.
5. parent Bubble RegionId와 같은 candidate를 역으로 찾음.
6. block:
   RegionTextRegion = detector TextBubble
   RegionId = detector parent Bubble
   RegionContainer = detector parent Bubble candidate
7. legacy UnitOwnership CandidateId가 다르면 detector candidate id로 교체.
8. 해당 unit의 LineOwnership CandidateId도 동일하게 교체.

이 변경으로 page017 / page028처럼
실제 OCR geometry는 오른쪽/아래 TextBubble인데
legacy candidate가 왼쪽/위 Bubble을 가리키는 오류를 차단한다.

관련 커밋:
27ab9af94f4fc36a8ba1c49c0f13ca9cf4765d89
V2 0035: make detector TextRegion authoritative for OCR ownership


변경 2 - consolidation 단계도 candidate-independent detector owner 사용
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/OcrPipelineService.cs

0034 ConsolidateRegionOwnedUnits는 detector TextRegion 기준 grouping을 도입했지만,
FindBestTextRegion 자체가 candidate 내부만 검색했다.

0035:
consolidation의 detector owner 계산도 OCR block geometry -> 전체 RT-DETR TextBubble
직접 매칭으로 변경.

따라서 잘못된 candidate 때문에 두 다른 물리 말풍선이 같은 detector owner로
오판되는 가능성을 줄인다.


변경 3 - orphan recovery도 detector-first
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/OcrPipelineService.cs

기존:
각 candidate를 순회하며 그 candidate 안의 TextBubble을 찾고 후보 경쟁.

0035:
1. OCR block geometry에서 detector TextBubble을 먼저 직접 선택.
2. detector TextBubble의 parent Bubble을 찾음.
3. parent Bubble에 대응하는 candidate를 역으로 찾음.
4. 해당 candidate로 orphan ownership을 복구.

candidate competition이 detector owner를 결정하지 않도록 방향을 역전.


변경 4 - secondary OCR도 detector-authoritative Bubble에 연결
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/OcrPipelineService.cs

기존 ApplySecondaryEvidence:
UnitOwnership.CandidateId -> candidate.RegionId를 사용해 Baberu evidence를 선택.

문제:
ownership을 detector 기준으로 고쳐도 legacy candidate가 남아 있으면
이웃 Bubble의 Baberu 문장이 다시 unit에 붙을 수 있음.

0035:
evidence region 선택 우선순위:
1. units[i].RegionId (detector-authoritative parent Bubble)
2. candidate.RegionId fallback

목표:
page017 오른쪽 문장에 왼쪽 "COME ON, COME ON." Baberu evidence가 붙는 문제 차단.

관련 커밋:
24e42ab4ed61f31779cfebd397c7f7467b7537a5
V2 0035: attach secondary OCR to detector-authoritative Bubble


변경 5 - page021 Render=false recovery 보강
------------------------------------------------------------
파일:
src/LocalMangaTranslator/Services/PagePipelineService.cs

0034 조건:
runtime Source.RegionTextRegion이 직접 존재해야 recovery 가능.

0035:
직접 RegionTextRegion link가 없더라도:
- Source.RegionId가 RT-DETR Bubble과 일치하고
- 그 Bubble 아래 detector-parent mapping 기준 TextBubble이 정확히 1개라면
해당 TextBubble을 구조적으로 다시 연결.

이후 기존 safety 조건:
- Type = dialogue / thought / caption
- meaningful text
- secondary OCR evidence 또는 primary 평균 confidence >= 0.80
을 만족하면 Render=true로 재활성화.

재활성화 후에도 final translator / V2 selector / layout / erase /
cleaned-state 검수를 모두 통과해야 commit.

관련 커밋:
542b01c54e88b7db1123437ab4b6b5d4296baf01
V2 0035: recover unique detector TextBubble from parent Bubble


변경 6 - TextFree/SFX 원본 보존 정책 명시
------------------------------------------------------------
파일:
src/LocalMangaTranslator/PipelineV2/Erase/V2EraseSelector.cs

사용자 결정:
SFX / effect / decorative free text는 번역·삭제하지 않고 원본 artwork로 보존.
애매한 free-standing text도 artwork 손상보다 보존 우선.

0035:
- 모든 RT-DETR TextFree target을 기본 preservation target으로 등록.
- 기본 target reason = textfree_original.
- translation이 TextFree에 매칭돼도 erase binding을 만들지 않음.
- stylized heuristic이 명확하면 기존 stylized_graphic reason 유지 가능.
- mixed fallback이 발생해도 TextFree는 제거하고 TextBubble만 erase 대상으로 진행.

효과:
page012 "LET ME OUTTT!" 같은 SFX가
unresolved가 아니라 preserved_policy로 coverage에 기록되고,
향후 stochastic translation result 때문에 erase 대상으로 다시 들어가는 것을 방지.

관련 커밋:
498f335932397df82d99ba46feb4f318d42c5609
V2 0035: preserve TextFree targets as original artwork


Regression tests
------------------------------------------------------------
파일:
tests/PipelineChecks/Program.cs

추가/변경:
1. legacy RegionId가 왼쪽 Bubble을 가리켜도
   OCR geometry가 오른쪽 TextBubble 내부이면 오른쪽 detector TextRegion을 선택.

2. RegionTextRegion 직접 link가 없어도
   parent Bubble 아래 unique TextBubble이 있으면 Render=false dialogue recovery.

3. TextFree caption/dialogue/stylized lettering 모두 erase binding을 만들지 않고
   original preservation reason을 가져야 함.

4. 기존 stylized short shout는 stylized_graphic reason 유지.

관련 커밋:
c36a59fe4f5a4a2f1a39f1569815e6e384f39803
Refactor 0035 detector text owner resolver for regression tests

c98ba4a9e22c9052a433a53ff9dbc3e46ee2b122
Tests: lock 0035 detector ownership recovery and TextFree preservation


0035에서 의도적으로 건드리지 않은 것
------------------------------------------------------------
- 0033 FLAT_FILL / TELEA reconstruction
- chromatic residual cleanup
- CleanedStateVerifier 판정 방식
- RT-DETR model threshold / detector scale sensitivity
- final Korean translation style

따라서 page028 "ANYTHING."의 cleaned_text_redetected는
0035 ownership 변경 후에도 남으면 별도 erase/verifier 문제로 다룬다.

page014 NONONO의 resolution/scale detector miss도 별도 문제로 유지.


0035 우선 재검증 페이지
------------------------------------------------------------
page012
- Nononono 작은 말풍선 정상 번역 유지.
- LET ME OUTTT! TextFree는 원본 보존.
- target coverage에서 TextFree가 preserved_policy로 기록.

page017
- COME ON / IT HAS TO BE HERE가 서로 다른 detector TextBubble owner.
- 오른쪽 말풍선 영어가 남지 않아야 함.
- RG008 unresolved = 0 기대.

page021
- I DON'T KNOW ANYMORE... Render=false recovery.
- RG014 translated_committed 기대.

page028
- I'LL TELL YOU...와 SO FIRST...가 서로 다른 detector owner.
- RG024 unresolved = 0 기대.
- "ANYTHING." cleaned redetected는 별도 관찰.

page032
- AM I RIGHT? 0034 성공 상태 유지.
- weak 1 La 후보가 exact candidate를 제거하지 않아야 함.


남은 문제 / 다음 후보
------------------------------------------------------------
1. page014 detector scale sensitivity
2. page028 ANYTHING cleaned_text_redetected
3. 0035 재검증에서 target coverage unresolved가 새로 남는지 확인
4. detector owner가 없는 genuine OCR-only dialogue 처리 정책


============================================================
0036 - Dual Track Experiment / LayaExperimental Shadow Track
============================================================

목표
------------------------------------------------------------
0035까지의 Classic 판단 파이프라인을 유지하면서,
Laya를 별도 System-1 decision track으로 추가한다.

0036 초기 단계는 Shadow Mode다.
즉 실제 번역/erase/typeset 결과는 Classic 규칙이 계속 결정하고,
Laya는 같은 evidence를 별도로 판정해 비교 로그만 남긴다.

이 단계에서 Laya는 이미지/Vision/OCR 자체를 수행하지 않는다.
Laya의 역할은 detector/OCR/Vision 결과를 바탕으로 빠른 분류/판정만 수행하는 것이다.

구조
------------------------------------------------------------
PipelineDecisionTrack:
- Classic
- LayaExperimental

LayaDecisionMode:
- Off
- Shadow
- Advisory
- Active

0036 기본값:
- DecisionTrack = LayaExperimental
- LayaMode = Shadow

현재 실제 적용은 Shadow만 사용한다.
Advisory / Active enum은 후속 실험을 위한 예약값이며
0036에서 Classic 결과를 override하지 않는다.


Laya runtime
------------------------------------------------------------
공식 오픈소스 Laya 0.3.5 사용.
- upstream: NandhaKishorM/laya
- model: convaiinnovations/laya / typed-decisions subfolder
- license: Apache-2.0

C# 프로세스 내부에 모델을 직접 넣지 않고 Python sidecar worker를 사용한다.

신규 파일:
- tools/laya_bridge.py
- src/LocalMangaTranslator/Services/LayaDecisionService.cs

laya_bridge.py:
- laya typed-decisions checkpoint를 최초 1회 로드
- stdin/stdout JSONL 프로토콜
- worker를 페이지마다 재시작하지 않고 프로세스 수명 동안 checkpoint 유지
- Laya 내부 stdout은 stderr로 redirect하여 JSONL protocol 오염 방지
- CUDA 요청 후 Laya 자체 fallback으로 CPU 사용 가능

LayaDecisionService:
- worker 시작/유지
- Python에서 laya import 실패 시 laya==0.3.5 자동 설치
- LMT_LAYA_PYTHON 환경변수로 Python interpreter override 가능
- LMT_LAYA_DEVICE 환경변수로 device override 가능
- 기본 device 요청은 cuda
- Laya 실패는 Classic 결과를 실패시키지 않는다
- 실패 시 debug summary에 Available=false / Error 기록

최초 실행 시:
1. Python 존재 확인
2. laya package 확인
3. 필요 시 pip install laya==0.3.5
4. Hugging Face typed-decisions checkpoint 다운로드/로드
5. 이후 같은 worker를 재사용

중요:
Portable ZIP에는 모델 weight 자체를 포함하지 않는다.
모델은 최초 실행 시 upstream에서 내려받는다.


0036 Shadow decision 종류
------------------------------------------------------------

1. ownership
OCR block이 어느 immutable RT-DETR TextBubble에 속하는지 Laya가 별도로 판정.

입력 evidence:
- source / corrected text
- primary OCR 평균 confidence
- secondary OCR text/agreement
- current Classic owner
- OCR block geometry
- 근접 RT-DETR TextBubble 후보 최대 4개
- 각 후보 parent Bubble
- detector score
- block coverage
- center containment
- center distance

Laya choice:
- 실제 TextRegionId
- UNBOUND

중요:
Laya가 geometry를 만들 수 없다.
선택 가능한 owner는 RT-DETR이 이미 검출한 immutable TextBubble뿐이다.


2. detector_retry
Classic 기준 UNBOUND이고,
주변에 안전한 RT-DETR TextBubble 후보가 없는 OCR unit에 대해
다른 detector scale 재검사가 필요한지 noul(true/false)로 판정.

이 판단 역시 geometry 생성 권한이 없다.
후속 multi-scale detector 실험을 위한 triage signal만 기록한다.

page014 NONONO처럼:
- OCR은 강하게 존재
- 번역도 가능
- RT-DETR target 자체는 사라짐
인 케이스가 이 로그의 주요 대상이다.


3. render_recovery
Vision이 dialogue/thought/caption을 Render=false로 반환한 경우,
detector/OCR evidence를 바탕으로 재활성화할지 Laya가 별도 판정.

Classic의
PagePipelineService.ShouldRecoverDetectorOwnedRenderableUnit()
결과와 비교해 Match/Mismatch 기록.

page021
"I DON'T KNOW ANYMORE. I DON'T--"
같은 케이스가 주요 대상이다.


4. duplicate
같은 physical Bubble RegionId 아래에 여러 번역 candidate가 존재할 경우,
어떤 candidate를 살릴지 Laya choice로 판정.

Classic winner:
RenderPipelineService의 실제 V2CandidateKeepScore 기준.

Laya evidence:
- source/corrected text
- secondary OCR agreement
- OCR confidence
- detector TextRegion link

page032 "AM I RIGHT?" 약한/강한 후보,
page012 혼합 OCR 잔재 같은 케이스 비교 목적.


Shadow log
------------------------------------------------------------
각 페이지 debug 폴더에 추가:

- <page>.laya_decisions.json
- <page>.laya_summary.json

laya_decisions.json:
- Schema = pipeline-v2-laya-shadow-v1
- SourceFile
- Track
- Mode
- Runtime
- Available
- Error
- Decisions

각 decision:
- Kind
- RegionId
- RuleDecision
- LayaDecision
- Confidence
- Match
- Status
- Evidence

Shadow에서는 항상 Status=shadow_only이며
LayaDecision은 실제 pipeline action에 반영하지 않는다.


laya_summary.json:
- Schema = pipeline-v2-laya-summary-v1
- TotalDecisions
- Matched
- Mismatched
- NoAnswer
- ByKind
  - Compared
  - Matched
  - Mismatched
  - NoAnswer
  - MeanConfidence


Portable packaging
------------------------------------------------------------
LocalMangaTranslator.csproj에서
tools/laya_bridge.py를 output/tools/laya_bridge.py로 복사.

따라서 Portable build에서도 동일 bridge를 실행할 수 있다.


UI / 실행 로그
------------------------------------------------------------
작업 시작 로그에 현재 판단 track을 표시:

판단트랙: LayaExperimental/Shadow

사용자는 현재 테스트 빌드가 Classic-only인지
Laya shadow track인지 로그에서 바로 확인 가능.


Regression / CI
------------------------------------------------------------
PipelineChecks에 0036 기본 설정 고정:
- PipelineDecisionTrack.LayaExperimental
- LayaDecisionMode.Shadow

CI에서는 실제 Laya model을 다운로드하거나 inference하지 않는다.
빌드/회귀 테스트는 sidecar integration의 C# 구조만 검증한다.

실제 Laya accuracy/latency 평가는 사용자 로컬 RTX 4070 환경에서
0036 회귀 페이지를 돌린 뒤 laya_decisions/laya_summary 로그로 비교한다.


1차 실전 검증 페이지
------------------------------------------------------------
- page012
- page014
- page017
- page021
- page028
- page032

관찰 포인트:

page012
- TextFree 원본 보존 유지
- 혼합 OCR duplicate 후보에서 Laya winner 확인

page014
- NONONO unbound에 대해 detector_retry 판정 확인
- 아직 geometry는 Classic RT-DETR 1회 결과만 사용

page017
- 0035에서 고친 ownership을 Laya가 동일하게 선택하는지 확인

page021
- "I DON'T KNOW ANYMORE..." Render=false에 대해
  Classic recovery와 Laya recovery 판단 비교

page028
- "SO FIRST..." ownership 정상 유지 확인
- 남은 ANYTHING cleaned-state 문제는 0036 Laya shadow가 직접 수정하지 않음

page032
- "AM I RIGHT?" exact candidate를 Laya도 선택하는지 확인


0036에서 의도적으로 하지 않은 것
------------------------------------------------------------
- Laya가 Classic 결정을 override하지 않음
- Laya가 새 Bubble/TextBubble geometry를 생성하지 않음
- RT-DETR multi-scale inference 아직 미적용
- cleaned-state verifier 판정을 Laya가 변경하지 않음
- Laya가 번역문을 생성하지 않음
- final Korean translation 모델 변경 없음


관련 커밋
------------------------------------------------------------
7a7351f2c4257956d7839bfb20602b8454a7ba11
V2 0036: add persistent Laya decision bridge

f89883a3a540fb60c6339f978f16f0bf0d4623aa
V2 0036: add Laya shadow decision service

0cc1b7811b27357a8f4af36de98dcd5091103ff2
Refactor 0036: expose classic duplicate score for shadow comparison

d374e31cb07390ef0f5c504ceb1138dad7b75bce
V2 0036: add dual-track Laya shadow options

3ec17b3c78e42b4bfa8b95e4f4ee1df456971c82
Build 0036: package Laya bridge with portable app

02346793cf8e21fdc2fd678795ba8dcffc9a7c8e
V2 0036: run Laya experimental shadow track per page

3b29aaae446c3e09e120acfb93f0c35cfa532000
UI 0036: show active Laya experimental track in run log

84ba248c459a4182bfa7b4ed1ddedcd913af5de1
Docs 0036: add Laya third-party notice

951a85067112a7bf643f20b1791ad29a55f20e1c
Tests: lock 0036 Laya shadow track defaults

============================================================
0037 - Laya Runtime Bootstrap / Model Check Preflight
============================================================

배경
------------------------------------------------------------
0036 첫 실전 회귀 테스트(page012/014/017/021/028/032)에서
최종 이미지 결과는 0035와 동일 계열이었으나,
Laya shadow log는 전 페이지에서 실제 inference를 한 번도 수행하지 못했다.

0036 test output:
- page012/014/017/021/028/032 모두
  Available = false
  Runtime = null
  TotalDecisions = 0
- 공통 오류:
  Windows에서 'python' 실행 파일을 찾지 못함.
- 따라서 당시 빠른 처리 속도는 Laya inference가 추가된 결과가 아니라
  Laya bootstrap이 즉시 실패하여 Classic-only로 처리된 결과였다.
- 0035 대비 0036 출력 파일은 줄지 않았고,
  기존 파일 누락 0개 + Laya decision/summary 12개가 추가된 상태였다.

0037 목표
------------------------------------------------------------
사용자가 Python/Laya를 별도로 수동 설치하지 않아도
[선택 모델 확인/설치] 단계에서 LayaExperimental Shadow Track까지
미리 준비할 수 있도록 Windows runtime bootstrap을 보강한다.

핵심 원칙은 그대로 유지한다.
- Laya는 Shadow에서 Classic 결과를 override하지 않는다.
- Laya bootstrap 실패가 번역/erase/typeset Classic 결과를 실패시키지 않는다.
- geometry 생성 권한은 여전히 없다.


Python runtime discovery
------------------------------------------------------------
기존:
- LMT_LAYA_PYTHON 또는 고정 명령 'python'만 사용.

0037:
다음 후보를 순서대로 probe한다.

1. LMT_LAYA_PYTHON
2. python
3. py -3
4. python3
5. LocalAppData 표준 설치 경로
   - Python313
   - Python312
   - Python311
   - Python310
6. Program Files 표준 설치 경로
   - Python313
   - Python312
   - Python311
   - Python310

각 후보는 실제 Python code를 실행해
Python >= 3.10인지 확인한 뒤에만 선택한다.

Laya 0.3.5 upstream package metadata 기준:
- requires-python >= 3.10
- Python 3.10 / 3.11 / 3.12 / 3.13 지원 표기.

선택한 runtime은 프로세스 수명 동안 cache하여 재사용한다.


Laya package version pin
------------------------------------------------------------
0036은 import laya가 성공하면 설치된 버전을 그대로 사용했다.

0037은 import 성공 여부뿐 아니라
__version__ == 0.3.5인지 확인한다.

다른 버전이 설치되어 있거나 import가 실패하면:
pip install laya==0.3.5

를 실행하여 실험 버전을 고정한다.


선택 모델 확인/설치 연동
------------------------------------------------------------
MainWindow의 [선택 모델 확인/설치] 흐름에
Laya preflight를 추가했다.

순서:
1. Ollama / 선택 Vision·번역 모델 확인 및 설치
2. RT-DETR 확인/설치
3. Baberu 확인/설치
4. Laya Python runtime 확인
5. laya==0.3.5 확인/설치
6. typed-decisions checkpoint 로드
7. worker ready 확인

Python 3.10+ runtime을 찾지 못하면
Python 3.12 자동 설치 여부를 묻는다.

사용자가 동의하면 winget:
Python.Python.3.12

를 설치한 뒤 Laya 준비를 다시 시도한다.

사용자가 Python 설치를 취소해도
Classic pipeline 자체는 계속 사용할 수 있다.


Persistent worker 재사용
------------------------------------------------------------
0036에서는 PagePipelineService 내부에서
LayaDecisionService를 자체 생성했다.

이 구조에서는 모델 확인 단계에서 별도 Laya instance를 준비해도
실제 페이지 처리 시 다른 instance가 checkpoint를 다시 로드할 수 있다.

0037:
- MainWindow가 LayaDecisionService 한 개를 소유.
- 같은 instance를 PagePipelineService에 주입.
- [선택 모델 확인/설치]에서 준비한 worker를
  실제 page012/... 처리에서도 그대로 재사용.

따라서 checkpoint를 model-check에서 한 번 로드한 뒤
페이지마다 다시 로드하지 않는다.


TensorFlow import deadlock 방어
------------------------------------------------------------
Laya upstream은 transformers가 TensorFlow를 probe하는 환경에서
model construction이 멈출 수 있으며 USE_TF=0 사용을 권장한다.

0037:
- Python package/version probe subprocess
- persistent laya_bridge worker
- laya_bridge.py import 이전

모두 USE_TF=0을 적용한다.


UI / 로그
------------------------------------------------------------
[선택 모델 확인/설치] 실행 중 다음 상태를 표시한다.

- Laya Python 확인
- Laya 0.3.5 설치/버전 고정
- typed-decisions checkpoint 로딩
- LayaExperimental 준비 완료

준비가 완료되면 실제 runtime description에
사용된 Python launcher/version + Laya version + device를 기록한다.

예:
py -3 · Python 3.12.x · laya 0.3.5 · typed-decisions · cuda


Regression / CI
------------------------------------------------------------
PipelineChecks에 live model download/inference 없이
Python discovery 구조만 고정했다.

검증:
- LMT_LAYA_PYTHON override가 최우선.
- Windows py -3 launcher fallback 존재.
- 표준 Python312 LocalAppData 경로 포함.

실제 Laya package/model download는 CI에서 수행하지 않는다.


0037 재검증 페이지
------------------------------------------------------------
0036에서 Laya가 실행되지 않았으므로 같은 6페이지를 다시 사용한다.

- page012
- page014
- page017
- page021
- page028
- page032

이번 검증의 1차 성공 조건:
- 각 page .laya_summary.json의 Available = true
- Runtime != null
- TotalDecisions > 0인 페이지가 실제로 존재
- Decisions가 shadow_only 상태로 기록
- Classic 최종 결과는 Laya 때문에 변경되지 않음

그 뒤에 ownership / detector_retry / render_recovery / duplicate의
Match/Mismatch와 confidence를 비교한다.


0037에서 의도적으로 하지 않은 것
------------------------------------------------------------
- Laya Advisory/Active 전환 없음.
- Laya 판단의 Classic override 없음.
- multi-scale RT-DETR 실제 재검출 없음.
- OCR consensus / translation consistency 전용 Laya decision은 아직 추가하지 않음.
- page014 detector scale 문제 자체 수정 없음.
- page028 cleaned_text_redetected 문제 자체 수정 없음.


관련 커밋
------------------------------------------------------------
89d0b4657c55a9c96442a65d7da23d27f7d4c681
V2 0037: harden Laya runtime bootstrap

============================================================
0038 - Windows Hugging Face No-Symlink Cache for Laya
============================================================

배경
------------------------------------------------------------
0037에서 Python 3.12 자동 설치와 laya==0.3.5 설치까지는 정상 동작했다.

사용자 실제 로그:
- Python 3.12.10 자동 설치 성공
- laya 0.3.5 설치 성공
- typed-decisions checkpoint 로딩 시작
- 이후 Hugging Face cache snapshot 생성 중 WinError 1314 발생

오류 형태:
blobs/<hash> -> snapshots/<revision>/typed-decisions/encoder/config.json
심볼릭 링크 생성 권한이 없어 checkpoint 준비 실패.

Windows에서는 Hugging Face cache가 기본적으로 symlink를 사용할 수 있고,
Developer Mode 또는 관리자 권한이 없으면 이 작업이 실패할 수 있다.


0038 목표
------------------------------------------------------------
사용자에게 Windows Developer Mode나 관리자 실행을 요구하지 않고
일반 데스크톱 권한으로 Laya checkpoint를 준비한다.


Hugging Face no-symlink cache
------------------------------------------------------------
Laya Python subprocess와 persistent worker에 다음 환경변수를 강제한다.

HF_HUB_DISABLE_SYMLINKS=1
HF_HUB_DISABLE_SYMLINKS_WARNING=1

의미:
- snapshot directory에 symlink 대신 실제 파일을 복사/배치.
- Windows symlink privilege가 없어도 checkpoint를 사용할 수 있음.
- 동일 blob의 중복 저장 가능성은 있으나 portability를 우선.

laya_bridge.py에도 같은 값을 import 이전에 설정하여
bridge를 직접 실행하는 경우에도 동일 동작을 보장한다.


Python diagnostic UTF-8
------------------------------------------------------------
0037 실패 로그에서 Windows 로컬라이즈 오류 문자열이 깨져 보였다.

0038에서 모든 Laya Python subprocess에:
PYTHONUTF8=1
PYTHONIOENCODING=utf-8

을 설정한다.

이후 Python/Hugging Face 오류 메시지를 debug log에서
UTF-8로 읽을 수 있도록 한다.


공통 환경 설정
------------------------------------------------------------
LayaDecisionService.ConfigurePythonEnvironment(ProcessStartInfo)를 추가.

다음 모두 동일 환경을 사용한다.
- Python runtime probe
- pip/laya version check
- persistent laya_bridge worker

설정:
- USE_TF=0
- HF_HUB_DISABLE_SYMLINKS=1
- HF_HUB_DISABLE_SYMLINKS_WARNING=1
- PYTHONUTF8=1
- PYTHONIOENCODING=utf-8


Regression / CI
------------------------------------------------------------
PipelineChecks 추가:
- HF_HUB_DISABLE_SYMLINKS == 1
- PYTHONUTF8 == 1
- PYTHONIOENCODING == utf-8

실제 Hugging Face model 다운로드는 CI에서 수행하지 않는다.


0038 재검증
------------------------------------------------------------
[선택 모델 확인/설치]을 다시 누른다.

기대:
1. 기존 Python 3.12.10 재사용
2. 기존 laya 0.3.5 재사용
3. typed-decisions checkpoint 재시도
4. symlink 대신 no-symlink cache로 snapshot 준비
5. LayaExperimental 준비 완료 로그 출력

기존 실패에서 내려받은 blob은 Hugging Face cache에 남아 있을 수 있으므로
가능한 경우 재사용되며 전체 checkpoint를 처음부터 다시 받을 필요는 없다.


관련 커밋
------------------------------------------------------------
bf50b687c7368314ac454d26b9882ee6b8f1a1f2
V2 0038: disable HF symlink cache for Laya

============================================================
0039 - Laya Shadow Coverage Completion / Language Evidence Track
============================================================

0038 실전 Shadow 결과
------------------------------------------------------------
검증 페이지:
- page012
- page014
- page017
- page021
- page028
- page032

Laya runtime:
- Python 3.12.10
- laya 0.3.5
- typed-decisions
- 실제 runtime device = cpu

6페이지 전체:
- TotalDecisions = 86
- Matched = 79
- Mismatched = 7
- NoAnswer = 0
- 전 페이지 Available = true / Error = null

0038에서 확인한 장점:
- page014 NONONO NNNNOOO!
  - OCR confidence 약 0.926
  - RT-DETR TextBubble candidate 0
  - Classic detector_retry = false
  - Laya detector_retry = true
  - confidence 약 0.587
- page012 혼합 OCR / LET ME OUTTT 계열에서도 detector_retry=true 신호 발생.

0038에서 확인한 제한:
- ownership은 page017에서 4건 mismatch.
  강한 실제 owner 대신 주변 약한 후보를 선택한 경우가 있어
  Active/override 대상으로 사용하지 않는다.
- page028에서도 UNBOUND를 약한 근접 candidate에 연결하려는 ownership mismatch 확인.
- 따라서 ownership은 계속 Shadow diagnostic 전용.

Shadow 결과 불변성:
- page012/014/017/021/028은 0036과 최종 이미지 SHA까지 동일.
- page032 차이는 erase가 아니라 Gemma 최종 번역/조판 text 변화.
- Laya Shadow가 결과 이미지를 변경한 흔적 없음.


0038 계측 누락 문제
------------------------------------------------------------
1. render_recovery
   page021 RG014는 최종 target coverage에서
   preserved_render_false:dialogue 상태인데
   0038 Laya log에는 render_recovery decision이 0건이었다.

   원인:
   기존 hook은 raw Vision result 직후만 관찰했다.
   final translation refinement에서 다시 Render=false가 된 unit은 보지 못했다.

2. duplicate
   page012 실제 V2 commit에서
   Region 0이 original_preserved:container_duplicate로 억제되고
   Region 1이 같은 RG002 / parent Bubble RG001의 winner로 commit됐다.

   그러나 0038 Laya duplicate decision은 0건이었다.

   원인:
   기존 Shadow grouping은 Source.RegionId만 사용했다.
   Region 0처럼 Source.RegionId가 없어도
   V2 selector가 동일 immutable TextBubble/parent Bubble에 bind한 후보를 놓쳤다.


0039 목표
------------------------------------------------------------
Laya를 Active로 승격하지 않는다.

먼저 Shadow가 Classic/V2에서 실제로 일어나는 판단을 빠짐없이 관찰하게 하고,
OCR1 / OCR2 / Vision correction / final translation을 language evidence track으로 추가한다.


render_recovery 2-stage Shadow
------------------------------------------------------------
Stage 1:
stage = vision_pre_recovery

raw Vision에서 Render=false인 dialogue/thought/caption을 보고
Classic ShouldRecoverDetectorOwnedRenderableUnit()과 Laya를 비교한다.

Stage 2:
stage = post_translation_final

TranslationRefinement 이후에도 최종 Render=false로 남은
dialogue/thought/caption을 다시 Laya에 전달한다.

evidence:
- source/corrected text
- entered_translation_renderable
- final_render
- classic_recovery_eligible
- primary OCR confidence
- secondary OCR/agreement
- Bubble/TextBubble ids
- nearby immutable detector candidates

이 Stage 2가 page021 RG014 같은 최종 residual을 관찰한다.

Shadow이므로 Stage 2 Laya true도 실제 Render를 변경하지 않는다.


duplicate Shadow - V2 physical container 기준
------------------------------------------------------------
기존 Source.RegionId grouping 제거.

0039은 V2EraseSelection.Bindings를 사용한다.

physical owner key:
1. parent BubbleRegionId가 있으면 bubble:<id>
2. 없으면 immutable LayoutBounds key

같은 physical owner에 bind된 renderable translation candidate가 2개 이상이면
duplicate arbitration Shadow를 생성한다.

Classic winner:
RenderPipelineService.GetV2CandidateKeepScore()

Laya evidence:
- candidate source/corrected text
- secondary OCR agreement
- OCR confidence
- detector TextRegion
- V2 binding TextRegionIds
- V2 BubbleRegionId
- immutable LayoutBounds
- Classic keep score

page012 Region 0 / Region 1처럼
Source.RegionId가 한쪽에 없어도 동일 V2 physical container 경쟁을 기록할 수 있다.

PipelineChecks:
동일 immutable RG002 / Bubble RG001에 bind된 두 translation unit이
한 duplicate candidate group으로 묶이는 것을 고정한다.


신규 language evidence Shadow decisions
------------------------------------------------------------

1. ocr_consensus
primary OCR과 Baberu secondary OCR의 관계를 Laya가 별도 분류.

choice:
- agree
- partial
- disagree
- secondary_only
- unknown

Classic RuleDecision은 기존 SecondaryOcrAgreement.
Laya 결과와 Match/Mismatch 기록.


2. vision_correction
Source.Text와 CorrectedText가 실제로 달라진 unit만 대상.

질문:
Vision corrected text가 primary/secondary OCR evidence로 정당화되는가.

Laya:
- accept
- reject

Classic RuleDecision:
- accept

unsupported word invention / large hallucinated expansion /
stronger OCR evidence contradiction 여부를 Shadow로 검사한다.


3. translation_consistency
최종 Render=true이며 translation이 존재하는
dialogue/thought/caption 대상.

evidence:
- source OCR
- corrected source
- Korean translation
- secondary OCR/agreement
- type

질문:
한국어 번역이 corrected source 의미를 보존하는가.

검사 포인트:
- 의미 누락/추가
- negation
- 이름/고유명사
- 의도
- 문장 polarity

스타일 취향은 평가하지 않고 semantic faithfulness만 본다.

Classic RuleDecision:
- accept

Laya:
- accept
- reject

이 decision도 0039에서는 Shadow only.


Shadow schema
------------------------------------------------------------
0039:
- pipeline-v2-laya-shadow-v2
- pipeline-v2-laya-summary-v2

기존 decision 종류:
- ownership
- detector_retry
- render_recovery
- duplicate

추가:
- ocr_consensus
- vision_correction
- translation_consistency


0039에서 의도적으로 하지 않은 것
------------------------------------------------------------
- Laya Active/Advisory 승격 없음.
- ownership override 없음.
- detector_retry=true가 실제 RT-DETR 재실행을 발생시키지 않음.
- multi-scale RT-DETR은 다음 단계 후보.
- Laya가 번역을 생성하거나 수정하지 않음.
- Laya가 Vision corrected text를 직접 덮어쓰지 않음.
- page028 cleaned-state verifier 문제 수정 없음.
- CUDA 강제 수정 없음. 0038 runtime CPU 원인은 별도 확인 가능.


0039 재검증
------------------------------------------------------------
같은 6페이지 재사용:
- page012
- page014
- page017
- page021
- page028
- page032

확인 포인트:
1. page021에 post_translation_final render_recovery decision 생성.
2. page012에 V2 physical-container duplicate decision 생성.
3. ocr_consensus가 Baberu evidence가 있는 unit에 생성.
4. Vision 교정 unit에 vision_correction 생성.
5. 최종 dialogue/thought/caption에 translation_consistency 생성.
6. 최종 이미지 결과는 Shadow 때문에 변경되지 않음.
7. ownership mismatch는 기록만 하고 실제 owner는 Classic 유지.


관련 커밋
------------------------------------------------------------
3a9c2dcac178f74a974813d0e99391c156acec4b
V2 0039: complete Laya shadow decision coverage

============================================================
0040 - Laya Shadow Recovery Probes / Local-Scale RT-DETR Retry
============================================================

0039 실전 결과 요약
------------------------------------------------------------
같은 6페이지(page012/014/017/021/028/032)에서
Laya Shadow decision coverage를 확장한 결과:

- TotalDecisions = 259
- Matched = 169
- Mismatched = 90
- NoAnswer = 0
- 0038과 0039 최종 이미지 6장 모두 SHA 동일.

핵심 관찰:
- detector_retry는 0038과 동일하게 가장 유망한 구조 판단.
- page014 NONONO 계열은 Laya retry=true 유지.
- page021 RG014
  "I DON'T KNOW ANYMORE. I DON'T-"
  최종 Render=false 상태를 0039 post_translation_final hook이 관찰했고
  Laya render_recovery=true / confidence 약 0.534.
- duplicate는 page012에서 실제 V2 physical-container 경쟁을 관찰했으나
  Laya가 Classic winner와 반대 후보를 선택.
- vision_correction / translation_consistency는 reject 과다로
  Active 판단에 사용하지 않음.

따라서 0040은 Laya가 잘하는 "다시 봐야 할 곳을 라우팅"하는 역할을
실제 Shadow action으로 검증한다.


0040 원칙
------------------------------------------------------------
결정 권한은 여전히 Classic/V2에 있다.

Laya:
- detector_retry=true -> 추가 RT-DETR Shadow pass 요청
- post_translation_final render_recovery=true -> 별도 Shadow 번역/바인딩 probe 요청

하지만:
- 새 detector geometry를 원래 PageAnalysisResult에 합치지 않음.
- V2DetectionSnapshot을 변경하지 않음.
- 최종 Render/erase/typeset 입력을 변경하지 않음.
- Shadow probe 결과는 debug JSON으로만 저장.


Local-scale RT-DETR Shadow retry
------------------------------------------------------------
RT-DETR ONNX 입력은 640x640 고정이다.

전체 페이지를 단순히 다시 640x640으로 resize하면
대상의 상대 크기가 바뀌지 않아 실제 scale retry 의미가 약하다.

0040은 Laya가 detector_retry=true로 고른 OCR block 주변을
서로 다른 크기의 정사각 local window로 crop한 뒤
각 crop을 640x640 detector 입력으로 보낸다.

passes:
- local_2p40
  OCR block 최대 변 길이의 2.40배 context
- local_1p60
  OCR block 최대 변 길이의 1.60배 context

최소 crop side:
- 256 source pixels

효과:
작은 OCR text가 full-page detector 때보다
RT-DETR 입력에서 더 큰 픽셀 비율을 차지한다.

각 crop의 RT-DETR 결과 box는
원본 page 좌표로 다시 offset 변환한다.

Shadow region ID:
- SH_<pass>_xxx

원래 RGxxx namespace와 분리한다.


Shadow retry match
------------------------------------------------------------
추가 RT-DETR 결과 중 TextBubble만 recovery 후보가 된다.

OCR block과 새 TextBubble이:
- 충분히 overlap하거나
- 서로 center-related

일 때 Shadow recovery match로 기록한다.

여러 후보가 있으면:
- geometric overlap affinity
- detector score

를 합친 diagnostic affinity로 정렬한다.

이 affinity는 Shadow audit용이며
실제 ownership/erase 권한을 부여하지 않는다.


Detector retry output
------------------------------------------------------------
새 debug JSON:
<page>.laya_detector_retry.json

Schema:
pipeline-v2-laya-detector-retry-shadow-v1

기록:
- TranslationRegionId
- SourceText / CorrectedText
- LayaConfidence
- OCR block geometry
- pass id
- crop bounds
- context multiplier
- input pixels per source pixel
- detector RegionCount
- detector TextBubbleCount
- Recovered true/false
- BestMatch bounds/score/source/affinity
- 모든 matching candidate

문서 최상위:
- Requests
- RecoveredRequests
- UnrecoveredRequests
- MutationApplied = false


page014 검증 목표
------------------------------------------------------------
0039에서 Laya가 retry=true로 분류했던:
NONONO NNNNOOO!

에 대해
local_2p40 또는 local_1p60 pass에서
새 TextBubble match가 실제로 검출되는지 확인한다.

이 단계에서 새 TextBubble이 발견되어도
최종 이미지에는 아직 반영하지 않는다.


post-translation render recovery Shadow probe
------------------------------------------------------------
0039 page021에서 최종 Render=false인 RG014 계열에 대해
Laya가 render_recovery=true를 반환했다.

0040은 post_translation_final true request만 대상으로:

1. 해당 region을 Shadow copy에서 Render=true로 re-arm
2. translationRefiner를 해당 unit 1개에 별도 실행
3. 실제 Korean translation candidate 생성 여부 확인
4. 기존 immutable V2DetectionSnapshot에
   V2EraseSelector.Select()를 Shadow로 실행
5. TextRegion/Bubble binding 가능 여부 확인

본 translated list에는 결과를 넣지 않는다.

새 debug JSON:
<page>.laya_render_recovery.json

Schema:
pipeline-v2-laya-render-recovery-shadow-v1

기록:
- source/corrected text
- Laya confidence
- original final Render
- Shadow translation candidate
- candidate Render / Translation / Type
- V2 TextRegionIds
- V2 BubbleRegionId
- TextBounds / LayoutBounds
- LayoutMode
- WouldBecomeRenderable
- MutationApplied = false


LayaShadowAuditResult
------------------------------------------------------------
LayaDecisionService는 JSON 기록 외에
실행 가능한 Shadow request 목록을 PagePipelineService로 반환한다.

- DetectorRetryRequests
- RenderRecoveryRequests

runtime error 발생 시 actionable request는 Empty로 반환하여
Classic 결과에 영향을 주지 않는다.


RT-DETR 구조 변경
------------------------------------------------------------
RtdetrPageRegionAnalyzer:
- 기존 full-page Analyze 동작 유지.
- 공통 AnalyzeMat 경로로 detector inference 재사용.
- AnalyzeShadowRetry 추가.
- crop detector 결과를 source page 좌표로 복원.
- BuildShadowRetryWindows helper 추가.
- IsShadowRetryMatch / ShadowRetryAffinity helper 추가.

기존 full-page RT-DETR output과 V2 snapshot 동작은 변경하지 않는다.


PipelineChecks
------------------------------------------------------------
0040 regression:
- page014 크기와 유사한 OCR block에서 local retry window 2개 생성.
- 두 window 모두 full page보다 작음.
- 두 window 모두 OCR block center를 포함.
- 실제로 겹치는 TextBubble은 retry match로 판정.
- 무관한 TextBubble은 retry match에서 제외.

live RT-DETR model inference는 CI 기본 PipelineChecks에서 수행하지 않는다.


0040에서 하지 않는 것
------------------------------------------------------------
- detector retry 결과를 PageAnalysisResult에 merge하지 않음.
- 새 TextBubble에 실제 ownership 부여 안 함.
- retry geometry로 erase/layout 실행 안 함.
- render recovery candidate를 최종 translated list에 넣지 않음.
- Laya ownership/duplicate/semantic decisions Active 승격 없음.
- page028 cleaned-state 문제 수정 없음.


0040 다음 판단 기준
------------------------------------------------------------
같은 6페이지를 재실행해 확인:

1. page014 detector retry가 실제 TextBubble을 recover하는가.
2. false-positive retry 후보는 얼마나 생기는가.
3. page021 render recovery Shadow에서
   Korean translation + RG014 V2 binding이 실제로 만들어지는가.
4. 최종 결과 이미지는 0039와 동일해야 한다.

위 두 recovery probe가 안정적이면
0041에서 detector_retry만 제한적 Advisory로 승격할 수 있다.


관련 커밋
------------------------------------------------------------
164e52812fd5af3af121a4ad51ceb58d9f68e17c
V2 0040: execute Laya shadow recovery probes

7deb5eef81f9b934ab12f3e7b14422ff7869f107
Fix 0040 RT-DETR shadow retry session call

============================================================
0041 - Safe Detector Retry Advisory / Negation Validator Fix
============================================================

0040 실전 결과
------------------------------------------------------------
검증 커밋:
21dd6c7316836049a09394ab8c4d30d17d6c76cf
test pic 0040

page014:
Laya detector_retry=true였던
"NONONO NNNNOOO!"가 실제 local-scale RT-DETR에서 재검출됐다.

OCR block:
- x=974
- y=1034
- w=336.33
- h=189.67
- OCR confidence 약 0.926

local_2p40:
- TextBubble x=985 y=1035 w=323 h=182
- score 약 0.542

local_1p60:
- TextBubble x=986 y=1033 w=317 h=184
- score 약 0.616

두 scale이 거의 동일한 physical TextBubble을 독립적으로 검출했다.
기존 full-page detector에는 이 위치 TextBubble이 없었으므로
실제 detector miss recovery로 판단한다.

page012:
Laya detector_retry=true였던 혼합 OCR block도 local retry에서
TextBubble을 찾았지만 기존 full-page RG002와 사실상 동일했다.

기존 RG002:
- x=913 y=102 w=133 h=50

retry:
- 약 x=915 y=104 w=128 h=48

따라서 page012는 신규 geometry recovery가 아니라
기존 detector target 재발견으로 분류해야 한다.

page021:
Laya render_recovery=true인
"I DON'T KNOW ANYMORE. I DON'T-"를 Shadow에서 다시 Render=true로
translation refinement에 넣었으나 다시:

- Render=false
- Translation=""
- V2Binding=null

로 떨어졌다.

즉 RG014 geometry/binding보다 앞선
TranslationRefinementService의 translation validation이
실패 원인임을 확인했다.

0040 최종 결과 불변성:
- page012/014/017/021/028은 0039와 final image SHA 동일.
- page032만 Gemma 최종 번역 wording 변화로 binary 차이.
- page032 erase changed pixels는 동일.
- Laya Shadow probe가 기존 출력에 직접 mutation한 증거 없음.


0041 목표
------------------------------------------------------------
Laya 전체를 Active로 올리지 않는다.

0040에서 실전 검증된 detector_retry만
제한적 Advisory action으로 승격한다.

동시에 page021에서 확인된
DON'T KNOW 계열 Korean negation validator false reject를 수정한다.


기본 decision mode
------------------------------------------------------------
PipelineOptions 기본값:

DecisionTrack = LayaExperimental
LayaMode = Advisory

Advisory에서 실제 결과에 영향을 줄 수 있는 항목은
0041 detector retry recovery만이다.

다음 Laya decision은 계속 diagnostic/shadow 성격으로 유지:
- ownership
- duplicate
- ocr_consensus
- vision_correction
- translation_consistency
- render_recovery


Detector retry Advisory 승인 조건
------------------------------------------------------------
Laya detector_retry=true만 retry 대상이 된다.

추가 RT-DETR local passes:
- local_2p40
- local_1p60

Advisory candidate로 승인하려면 모두 만족해야 한다.

1. 두 local scale에서 모두 matching TextBubble 검출.
2. 각 TextBubble에 detector Bubble parent가 존재.
3. 두 scale TextBubble이 같은 physical region.
   - IoU >= 0.45
   - 또는 normalized center distance <= 0.22
4. 두 scale parent Bubble도 같은 physical region.
   - IoU >= 0.30
   - 또는 normalized center distance <= 0.28
5. 두 TextBubble score 중 낮은 값도 >= 0.45.
6. 기존 full-page TextBubble과 중복되지 않아야 함.
   - IoU >= 0.45 또는 smaller-overlap >= 0.75이면
     duplicate_existing_fullpage로 거부.

따라서 0040 실측 기준:
- page014 NONONO -> 신규 Advisory 후보
- page012 retry -> 기존 RG002 duplicate로 거부


Advisory geometry
------------------------------------------------------------
승인된 retry geometry는 별도 namespace 사용.

TextBubble:
LRT<TranslationRegionId>

신규 local parent Bubble:
LRB<TranslationRegionId>

Source:
ogkalu-rtdetr-v2-laya-advisory

local TextBubble이 기존 full-page Bubble 안에 안전하게 들어가는 경우에는
새 Bubble을 만들지 않고 기존 Bubble을 parent로 재사용한다.


적용 범위 제한
------------------------------------------------------------
Advisory TextBubble은 retry를 trigger한 translation unit 하나에만 연결한다.

다른 OCR/translation unit에 전파하지 않는다.
기존 full-page owner보다 우선하는 전역 geometry replacement도 하지 않는다.

해당 unit Source에만:
- RegionTextRegion = Advisory TextBubble
- RegionId = Advisory/existing parent Bubble

를 연결한다.


V2 commit boundary
------------------------------------------------------------
Advisory candidate는 즉시 immutable target set에 넣지 않는다.

candidate 하나씩:

1. 임시 raw region set에 Bubble/TextBubble 추가.
2. 임시 V2DetectionSnapshot 생성.
3. trigger unit 하나만 V2EraseSelector.Select() 실행.
4. 정확한 LRT target binding이 생성됐는지 확인.
5. binding 성공 시에만:
   - translated unit link commit
   - V2 snapshot commit
   - V2 selection target/binding commit

실패 candidate는 snapshot에 남기지 않는다.

목적:
Advisory binding 실패가 새로운 unresolved detector target으로
coverage audit에 남는 것을 방지한다.


Detector retry audit
------------------------------------------------------------
기존:
pipeline-v2-laya-detector-retry-shadow-v1

0041:
pipeline-v2-laya-detector-retry-advisory-v1

추가 기록:
- Mode
- TwoScaleAgreement
- CrossScaleTextIoU
- CrossScaleBubbleIoU
- MinimumTextScore
- ExistingFullPageDuplicate
- AdvisoryEligible
- Applied
- RejectReason
- AdvisoryTextRegion
- AdvisoryBubbleRegion
- AdvisoryEligibleRequests
- AppliedRequests
- MutationApplied

대표 reject reason:
- retry_not_recovered
- missing_two_scale_parent
- cross_scale_inconsistent
- weak_retry_score
- duplicate_existing_fullpage


page021 translation validator 수정
------------------------------------------------------------
기존 TranslationRefinementService.IsUsableTranslation()은
영어 source에 DON'T/NOT/NEVER 등의 explicit negation이 있으면
Korean translation에도 negation cue를 요구한다.

기존 cue:
안 / 못 / 아니 / 않 / 없 / 말 / 지마 / 절대 등

문제:
"I DON'T KNOW."
"I DON'T KNOW ANYMORE."

의 자연스러운 Korean translation:

- 몰라.
- 더는 모르겠어.

는 negation 의미가 보존됐지만 기존 cue에 걸리지 않아
정상 번역도 validation failure가 될 수 있었다.

0041 추가 cue:
- 모르
- 몰라

따라서:
- 모르겠어
- 모른다
- 모릅니다
- 몰라

계열이 explicit negation preservation으로 인정된다.

반대로:
"I DON'T KNOW." -> "알겠어."

같은 polarity reversal은 기존처럼 reject한다.


PipelineChecks
------------------------------------------------------------
0041 regression 추가:

1. 기본 Laya mode = Advisory.
2. page014 실측과 유사한 두 retry TextBubble box가
   cross-scale same physical region으로 인정.
3. page012 retry box와 기존 RG002 box는
   existing full-page duplicate로 판정.
4. page014 retry box는 다른 기존 TextBubble과 중복으로 오판하지 않음.
5. "I DON'T KNOW ANYMORE..." -> "더는 모르겠어." 통과.
6. "I DON'T KNOW." -> "몰라." 통과.
7. "I DON'T KNOW." -> "알겠어."는 계속 reject.


0041에서 하지 않는 것
------------------------------------------------------------
- Laya ownership override 없음.
- Laya duplicate winner override 없음.
- vision_correction / translation_consistency Active 적용 없음.
- render_recovery를 최종 translated list에 직접 강제 삽입하지 않음.
- detector retry가 한 scale에서만 보이면 적용하지 않음.
- 기존 full-page TextBubble 재발견은 적용하지 않음.
- page028 cleaned_text_redetected / unbound 문제 수정 없음.


0041 재검증
------------------------------------------------------------
동일 6페이지:
- page012
- page014
- page017
- page021
- page028
- page032

핵심 기대:

page012
- detector retry는 duplicate_existing_fullpage로 reject.
- 기존 RG002 결과 유지.

page014
- NONONO retry가 두 scale 합의.
- AdvisoryEligible=true / Applied=true 기대.
- LRTxxx + parent Bubble binding 생성.
- 기존 7 TextBubble 외 신규 target이 실제 commit되는지 확인.
- NONONO가 최종 번역/erase/typeset 경로에 들어가는지 확인.

page021
- translation validator 수정으로
  Shadow render recovery candidate가 더 이상 단순 negation cue 때문에
  탈락하지 않는지 확인.
- 실제 model wording에 따라 recovery success 여부 재평가.

나머지 페이지
- 기존 결과 회귀 여부 확인.


관련 커밋
------------------------------------------------------------
b3f4ba1dac66235809170988d11561b24336db8e
V2 0041: promote safe detector retry advisory

3574c175c4c5508c518200077568d770194474ac
Fix 0041 advisory geometry helpers

9263fd4c42336c29133248fd6ba31abe48d7c556
Harden 0041 advisory commit boundary

0041 실전 검증 결과
------------------------------------------------------------
검증 커밋:
f149851c888ae4bee00f5ab4e1591ea1ccc2bb59
test pic 0041

결론:
0041의 두 핵심 목표 모두 실제 최종 결과까지 성공.

1. page014 detector retry Advisory 성공
- Laya detector_retry request: 1
- RecoveredRequests: 1
- AdvisoryEligibleRequests: 1
- AppliedRequests: 1
- MutationApplied: true

NONONO NNNNOOO!:
- TwoScaleAgreement = true
- CrossScaleTextIoU = 0.97095
- CrossScaleBubbleIoU = 0.65367
- MinimumTextScore = 0.54227
- ExistingFullPageDuplicate = false
- AdvisoryEligible = true
- Applied = true

생성:
- TextBubble LRT006
  x=986 y=1033 w=317 h=184
  score=0.61580
- Bubble LRB006
  x=985 y=945 w=319 h=298

V2 target coverage:
- 기존 TextBubble 7개 -> effective target 8개
- LRT006 translated_committed
- DetectedTextTargets=8
- CommittedTargets=8
- PreservedTargets=0
- UnresolvedTargets=0
- CoverageComplete=true

V2 commit:
- Region 6 "NONONO NNNNOOO!"
- Translation = "안 돼, 안 돼 / 안 돼, 안 돼!"
- Bound=true
- LayoutApproved=true
- EraseClean=true
- Committed=true
- Status=translated

0040 -> 0041:
- page014 final image SHA 변경
- V2TranslatedUnits 7 -> 8
- V2EraseCommittedUnits 7 -> 8
- V2TypesetCommittedUnits 7 -> 8
- V2UnboundUnits 2 -> 1

즉 0040에서 Shadow로만 확인했던 detector miss가
0041에서 실제 최종 번역/erase/typeset 개선으로 연결됨.


2. page012 기존 geometry 재발견 차단 성공
- retry 자체는 두 scale 모두 recovery
- TwoScaleAgreement=true
- CrossScaleTextIoU=0.96459
- CrossScaleBubbleIoU=0.98925
- MinimumTextScore=0.87668
- ExistingFullPageDuplicate=true

결과:
- AdvisoryEligible=false
- Applied=false
- RejectReason=duplicate_existing_fullpage
- MutationApplied=false

기존 RG002 / duplicate suppression 동작 유지.
0040과 page012 final image SHA 동일.

즉 retry detector가 강하게 같은 영역을 재발견하더라도
기존 full-page target과 중복이면 Advisory가 새 geometry를 만들지 않는다.


3. page021 DON'T KNOW validator 수정 성공
0040:
- RG014 preserved_render_false:dialogue
- 최종 Render=false
- Shadow re-arm 후에도 validator가 다시 Render=false로 떨어뜨림.

0041:
Region 6:
- CorrectedText = "I DON'T KNOW ANYMORE. I DON'T-"
- Translation = "더는 모르겠어. 난 / 모른—"
- Render=true

V2 target RG014:
- BoundTranslationRegionIds=[6]
- Status=translated_committed

coverage:
- DetectedTextTargets=15
- CommittedTargets=15
- PreservedTargets=0
- UnresolvedTargets=0
- CoverageComplete=true

0040 -> 0041:
- V2TranslatedUnits 14 -> 15
- V2EraseCommittedUnits 14 -> 15
- V2TypesetCommittedUnits 14 -> 15
- page021 final image SHA 변경

0041에서는 post_translation_final render_recovery decision 자체가 0건.
의미:
validator fix 덕분에 해당 unit이 정상 translation path에서 살아남아
Laya recovery probe가 필요 없어짐.


4. 회귀 확인
0040 -> 0041 final image SHA:
- page012 동일
- page014 변경 (의도된 신규 detector recovery)
- page017 동일
- page021 변경 (의도된 validator fix)
- page028 동일
- page032 동일

즉 의도된 두 페이지만 최종 출력이 변경되고
나머지 4개 regression page는 binary 동일.


5. 남은 기존 문제
page028:
- RG010 unresolved_detected_no_translation_unit 유지
- RG026 preserved_cleaned_state_or_background_quality 유지
- V2CleanedTextRedetectedUnits=1
- V2UnboundUnits=1
- CoverageComplete=false

0041 detector Advisory / negation validator 작업과는 독립 문제.


6. 진단 로그 주의점
page014 final audit의 initial page-analysis metric:
- RegionCount=14
- TextRegionCount=7

은 Advisory 이전 full-page RT-DETR 결과를 표시한다.

실제 effective V2 target coverage에는:
- LRT006 추가
- DetectedTextTargets=8

이므로 기능상 문제는 아니지만,
향후 audit summary에 Advisory-added target count를 별도로 표시하면
초기 detector count와 effective V2 target count의 차이를 더 명확히 볼 수 있다.

============================================================
0042 - Cleaned-State Detector False-Positive Rescue
============================================================

대상
------------------------------------------------------------
page028 RG026:
"ANYTHING."

0041 실전 결과에서 이 unit은:
- OCR 정상
- Baberu agreement=agree
- CorrectedText="ANYTHING."
- Translation="뭐든지."
- Render=true
- RG026 / RG025 binding 정상
- LayoutApproved=true

까지 모두 통과했지만 cleaned-state verifier에서
RT-DETR이 같은 위치를 다시 TextBubble로 검출하여
최종 commit이 취소됐다.

0041 cleaned-state:
- Original RG026:
  x=225 y=1606 w=146 h=39
  score=0.8291
- cleaned residual detection:
  x=223 y=1604 w=148 h=43
  score=0.7757
- Status=TEXT_REDETECTED
- final status=original_preserved:cleaned_text_redetected

반면 erase pipeline 자체의 source-linked evidence:
- InitialMaskPixels=260
- SelectedResidualPixels=6
- SelectedResidualRatio 약 0.0231
- SelectedPersistentCorePixels=0
- SelectedPersistenceRatio=0
- PrimaryReview=pass
- Status=clean_after_first_pass

즉 원래 glyph evidence는 거의 사라졌는데
cleaned RT-DETR만 원래 TextBubble 크기와 비슷한
검은 배경/테두리 구조를 다시 TextBubble로 보는 상황이었다.


0042 원칙
------------------------------------------------------------
RT-DETR cleaned re-detection 단독으로 commit을 막지 않는다.

단, 무조건 완화하지 않고 기존 erase pipeline의
source-linked residual evidence가 매우 강하게 clean일 때만
detector-only false-positive rescue를 허용한다.

원본 immutable target geometry는 그대로 유지한다.


Detector-only false-positive rescue 조건
------------------------------------------------------------
CleanedStateVerifier.CanRescueDetectorOnlyRedetection() 추가.

다음 조건을 모두 만족해야 한다.

1. ErasePipelineV2.IsAuditClean(audit) == true
2. InitialMaskPixels >= 40
3. SelectedResidualPixels <=
   min(12, ceil(InitialMaskPixels * 0.03))
4. SelectedPersistentCorePixels == 0
5. SelectedPersistenceRatio <= 0.005
6. 대상이 TextBubble이어야 함

이 조건은 page028 RG026 실측:
- 260 initial
- 6 residual
- 0 persistent glyph

을 통과한다.

의미 있는 residual이 남거나,
source glyph persistence가 조금이라도 확인되거나,
기존 erase reviewer가 fail이면 rescue하지 않는다.


Cleaned-state 판정 변경
------------------------------------------------------------
기존:
RT-DETR residual match 존재
-> TEXT_REDETECTED
-> EmptyVerified=false
-> commit 차단

0042:
RT-DETR residual match 존재
+ detector-only rescue 조건 만족
-> EMPTY_OK_DETECTOR_ONLY_FALSE_POSITIVE
-> EmptyVerified=true
-> V2 commit 허용

RT-DETR residual detection 자체는 audit에서 삭제하지 않는다.
즉 검출 기록은 그대로 남기되
"실제 원문 glyph 잔존 증거가 없음"을 별도 상태로 기록한다.


Audit 확장
------------------------------------------------------------
schema:
pipeline-v2-cleaned-state-v2

VerifierMode:
cleaned_rtdetr_plus_erase_evidence

target별 추가 필드:
- DetectorOnlyFalsePositiveRescue
- LegacyInitialMaskPixels
- LegacySelectedResidualPixels
- LegacyResidualRatio
- LegacyPersistentGlyphPixels

문서 최상위 추가:
- DetectorOnlyFalsePositiveRescueCount

따라서 page028에서 RT-DETR residual detection count는
1로 남을 수 있지만 RG026은 EmptyVerified=true가 될 수 있다.


PipelineChecks
------------------------------------------------------------
0042 regression 추가:

1. page028 RG026 실측과 동일한 audit:
   - initial=260
   - residual=6
   - persistent=0
   - clean_after_first_pass
   -> detector-only rescue 허용

2. residual=20
   -> rescue 거부

3. persistent source glyph=2 / ratio=0.01
   -> rescue 거부

4. legacy erase status=review_required
   -> rescue 거부


0042에서 하지 않는 것
------------------------------------------------------------
- RT-DETR cleaned verifier 제거하지 않음.
- detector score threshold를 전역으로 낮추거나 무시하지 않음.
- source-linked erase reviewer fail을 override하지 않음.
- persistent glyph가 남은 경우 통과시키지 않음.
- page028 RG010 unresolved_detected_no_translation_unit 문제는 별도 유지.
- Laya detector Advisory 규칙 변경 없음.


0042 재검증
------------------------------------------------------------
같은 6페이지 테스트 권장.

핵심 기대:
page028 RG026
- ResidualDetections에는 RT-DETR box가 남아도 됨.
- DetectorOnlyFalsePositiveRescue=true 기대.
- Status=EMPTY_OK_DETECTOR_ONLY_FALSE_POSITIVE 기대.
- RG025 checkpoint EmptyVerified=true 기대.
- Region 17 ANYTHING translation committed 기대.
- "뭐든지." 최종 typeset 기대.
- V2CleanedTextRedetectedUnits 1 -> 0 기대.

page028 RG010
- unresolved_detected_no_translation_unit는 이번 패치 대상이 아니므로
  그대로 남을 수 있음.

나머지 regression pages
- 0041 결과 유지 확인.


관련 커밋
------------------------------------------------------------
d0fa327b3a0694984d6e375a2aed31424fe9ab4e
V2 0042: rescue detector-only cleaned-state false positives



0042 실전 검증 결과
------------------------------------------------------------
검증 커밋:
0e8ef3a60a60f358f8bcc5deb0a3e136f876a544
test pic 0042

결론:
0042의 detector-only cleaned-state false-positive rescue가
page028 RG026 "ANYTHING." 실전 결과에서 의도대로 동작했다.

1. page028 RG026 rescue 성공
- cleaned RT-DETR residual detection 자체는 그대로 1개 존재.
- Original RG026:
  x=225 y=1606 w=146 h=39
- cleaned residual:
  x=223 y=1604 w=148 h=43
  score=0.77567005
- DetectorOnlyFalsePositiveRescueCount=1
- RG026:
  EmptyVerified=true
  Status=EMPTY_OK_DETECTOR_ONLY_FALSE_POSITIVE
  DetectorOnlyFalsePositiveRescue=true
- source-linked erase evidence:
  InitialMaskPixels=260
  SelectedResidualPixels=6
  LegacyResidualRatio=0.0230769
  SelectedPersistentCorePixels=0
  SelectedPersistenceRatio=0
  PrimaryReview=pass
  ReviewReason=primary_residual_clean
  Status=clean_after_first_pass

즉 detector가 같은 검은/어두운 구조를 TextBubble로 재검출한 기록은 보존하면서,
실제 source glyph persistence가 없다는 erase evidence에 의해
detector-only false positive로 정상 rescue되었다.

2. RG025 checkpoint / cleaned-state 전체
- EraseTargetBubbleCount=14
- EmptyVerifiedBubbleCount=14
- BubbleCheckpointPass=true
- OverallPass=true
- RG025 checkpoint:
  TextRegionIds=[RG026]
  EmptyVerified=true
  Status=EMPTY_OK

0041에서는:
- EmptyVerifiedBubbleCount=13/14
- BubbleCheckpointPass=false
- OverallPass=false
였으므로 RG026 하나만 정확히 개선되었다.

3. "ANYTHING." 실제 최종 commit 성공
Region 17:
- SourceText="ANYTHING.
- Translation="뭐든지."
- Bound=true
- TextRegionIds=[RG026]
- BubbleRegionId=RG025
- LayoutApproved=true
- LegacyEraseReviewClean=true
- CleanedStateEmpty=true
- ReviewerDisagreement=false
- EraseClean=true
- Committed=true
- Status=translated

0041:
- original_preserved:cleaned_text_redetected
- Committed=false

0042:
- translated
- Committed=true

4. page028 전체 지표
0041 -> 0042:
- RenderApproved: 13 -> 14
- RenderRejected: 1 -> 0
- V2TranslatedUnits: 13 -> 14
- V2PreservedOriginalUnits: 3 -> 2
- V2EraseCommittedUnits: 13 -> 14
- V2TypesetCommittedUnits: 13 -> 14
- V2CleanedTextRedetectedUnits: 1 -> 0
- CommittedTargets: 13 -> 14
- PreservedTargets: 1 -> 0
- EraseTypesetCountMatch=true 유지
- MissingUnits=0 유지

page028 final image SHA는 0041과 달라졌으며,
이는 RG026 신규 erase/typeset commit에 따른 의도된 변경이다.

5. RG010은 의도대로 미수정
RG010:
- Status=unresolved_detected_no_translation_unit
- BoundTranslationRegionIds=[]
- Unresolved=true

따라서:
- DetectedTextTargets=15
- CommittedTargets=14
- PreservedTargets=0
- UnresolvedTargets=1
- CoverageComplete=false

0042 범위에서 제외하기로 한 RG010 문제만 그대로 남아 있다.

6. 0041 대비 회귀 확인
최종 translated image blob SHA 비교:

page012
- 0041 == 0042
- 3d91db2b93d88d756d1ab120519af609f228f05c

page014
- 0041 == 0042
- dce90a4c0deaec3baac264d9de1648fe387f0c8e

page017
- 0041 == 0042
- 7146bb9fa72a3aa09b1332246a5c8097a700f028

page021
- 0041 == 0042
- 3f1d8234c29bbe82b1e88a88ee2b6d0d80d7540a

page032
- 0041 == 0042
- b1d5f50ad4f28f6ac6aa0be84d86373dfc520b72

page028만:
- 0041 c2f8e0bbde12312b2e0f8e43a284d69ad050166c
- 0042 a497e9377c53f7e76f51c0fce5bbbc425f64f8c2

즉 0042는 목표였던 page028 RG026만 최종 결과를 변경했고,
나머지 5개 regression page는 binary 동일하다.

7. 0042 최종 판단
- detector-only false-positive rescue 실전 검증 성공.
- 실제 source glyph persistence가 없는 경우에만 RT-DETR 단독 재검출을 뒤집는 구조가 정상 작동.
- 기존 RT-DETR residual evidence는 audit에 유지됨.
- erase/typeset atomic count invariant 유지.
- 일반 페이지 회귀 없음.
- 다음 별도 과제는 page028 RG010 unresolved_detected_no_translation_unit.


0043 - full chapter regression fixes
------------------------------------------------------------
기준 실전 결과:
- test pic/0042_2
- commit ca1abe5ab37ada753fda216f0b39c523f64458d7

목표:
한 챕터 실전 테스트에서 확인된 3개 회귀 유형을 서로 독립적으로 수정한다.
1. page005: 번역 글자와 원문이 겹치는 말풍선 / erase 잔존
2. page016: 우측 하단 검은 배경 TextFree 문장 미번역
3. page031: 녹색 대사 번역 누락

1. page005 TextBubble residual retry 강화
관찰:
- page005 RG008
- TextBubble x335 y505 w185 h107
- FLAT_FILL
- InitialMaskPixels=7005
- raw residual=524
- effective residual=417
- effective/initial ~= 5.95%
- SelectedPersistentCorePixels=0
- 기존 primary review는 pass로 처리되어 retry가 실행되지 않았다.
- 최종 이미지에서는 원문 일부가 남아 새 한국어와 겹치는 현상이 관찰되었다.

변경:
- generic residual policy는 그대로 유지한다.
- immutable RT-DETR TextBubble에만 stricter primary gate를 추가한다.
- effective residual이 max(24, InitialMaskPixels * 0.05)를 초과하면
  첫 pass를 clean으로 확정하지 않고 retry로 보낸다.
- TextFree/caption/artwork용 기존 density tolerance는 변경하지 않는다.
- 목적은 정상 말풍선의 눈에 띄는 source glyph 잔존만 더 적극적으로 재삭제하는 것이다.

2. page016 sentence-like TextFree 번역 허용
관찰:
- 미번역 영역은 detector miss가 아니었다.
- RT-DETR이 이미 TextFree로 검출했다.
- 예:
  RG021 x980 y1585 w213 h138 score~0.906
  source:
  "...YOU'VE BEEN LOST IN THE DARK A LONG TIME. YOUR EYES JUST NEED TO ADJUST."
  OCR confidence ~0.98
- RG024 x1100 y1866 w137 h37 score~0.812
  source: "THAT'S ALL."
  OCR confidence ~0.996
- 둘 다 translation 자체는 존재하고 Render=true였지만,
  0035의 blanket TextFree preserve 정책 때문에
  original_preserved:textfree_original로 최종 보존되었다.

변경:
- TextFree는 계속 preserve-by-default.
- 다음 조건을 모두 만족하는 경우에만 immutable TextFree geometry를
  실제 erase + local layout 대상으로 허용한다.
  * detector target이 TextFree
  * parent Bubble 없음
  * type이 dialogue/thought/caption
  * OCR line 존재
  * 평균 OCR confidence >= 0.90
  * 의미 문자 >= 6
  * 공백이 있는 문장형 텍스트
  * '.', '?', ',', ';', ':' 중 하나를 포함
  * 기존 stylized graphic 보호 조건에 걸리지 않음
- mixed TextBubble/TextFree ownership 결합 금지 규칙은 그대로 유지.
- 허용된 TextFree는 layoutMode=rtdetr_textfree를 사용한다.
- 기존 decorative/SFX/짧은 비문장형 TextFree는 계속 원문 보존한다.

3. page031 negative question-tag validator 수정
관찰:
- 해당 unit source:
  WAIT THE "KNOCK-KNOCK" COMES FIRST, DOESN'T IT... SHIT.
- detector TextBubble RG006 score~0.947
- primary OCR confidence ~0.951
- secondary OCR agree
- Classic detector-owned recovery eligibility=true
- Laya post_translation_final render_recovery=true confidence=0.5609
- 즉 OCR/detector/Laya는 실제 대사라고 판단하고 있었다.
- 하지만 final translation validation에서 "DOESN'T"를 일반 부정문으로 보고
  한국어 결과에 안/못/않 등의 부정 cue가 없으면 반복적으로 reject했다.
- 실제 "doesn't it?"는 부정 의미가 아니라 부가의문문이므로
  "먼저잖아?" 같은 자연스러운 한국어 번역에 부정 cue가 필요하지 않다.

변경:
- HasExplicitEnglishNegation 검사 전에 comma-led negative question tag를 제거한다.
- 예:
  ", doesn't it"
  ", isn't it"
  ", aren't they"
  ", won't you"
  등 auxiliary contraction + pronoun 형태.
- 일반 문장 부정:
  "I DON'T KNOW"
  "I CAN'T"
  "NEVER"
  등은 기존 보호 규칙을 그대로 유지한다.

4. Laya 관련
- 0043에서 Laya render_recovery를 무조건 mutation으로 승격하지 않는다.
- page031 사례는 Laya 판단 자체는 맞았지만,
  근본 원인이 translation negation validator 오판이었으므로
  validator를 수정해 Classic pipeline이 정상적으로 완료되도록 한다.
- detector_retry Advisory 정책은 기존 0041 범위를 유지한다.

5. PipelineChecks 추가/변경
- page005-like TextBubble residual:
  generic policy에서는 허용되지만 strict TextBubble gate에서는 retry되는지 확인.
- negative question tag:
  "DOESN'T IT?"가 포함된 문장이 한국어 부정 cue 없이도 유효 번역으로 인정되는지 확인.
- 기존 "I CAN'T." 의미 반전 방지 테스트는 그대로 유지.
- TextFree:
  비문장형/스타일형 preserve 테스트는 유지.
  "THAT'S ALL." 같은 strong sentence-like TextFree는 rtdetr_textfree binding으로
  승격되는지 확인.

0043 실전 재검증 권장:
- 우선 page005 / page016 / page031 3페이지만 재실행.
- 이후 회귀가 없으면 동일 챕터 전체 재실행.
- page005: 원문 잔존/겹침 해소 확인.
- page016: 우측 하단 검은 배경 문장 실제 erase + 한국어 조판 확인.
- page031: 녹색 대사 Render=true + 최종 한국어 commit 확인.


0043 CI verification
------------------------------------------------------------
Final code head:
- ac2321186aaf08c9b950a9816d9bc2be801c2bf6

Pipeline V2 Windows Portable:
- run 35731425929
- conclusion: success
- Pipeline evidence checks: success
- Publish win-x64: success

0043 implementation commits:
- 5b8250dc6dd00440e47fa04504423c5663108ba7
  TextBubble residual retry gate
- d00e742f0a59b2838cdda19c7f388165d13cb0c2
  safe sentence-like TextFree translation
- 273102bb878db563f36bf3e28a19563bfdee0d97
  negative question-tag validator
- f15521dc1b1cf8643afe1cad8bc1464d0bc85b52
  PipelineChecks
- ac2321186aaf08c9b950a9816d9bc2be801c2bf6
  final question-tag regex escaping fix


0044 - independent flat-background residual verifier
------------------------------------------------------------
기준 실전 결과:
- test pic/0043
- commit 396adecb73fe508d9b36f5badd0c907c932288f9

문제:
- page005 RG008은 0043에서 retry가 실제 실행되어
  SelectedResidualPixels 417 -> 10까지 감소했다.
- status=clean_after_retry
- cleaned-state verifier도 OverallPass=true
- 그러나 실제 결과 이미지에서는 원문 일부가 남아 한국어와 겹쳤다.
- 즉 기존 검수기가 erase mask / residual mask가 놓친 source glyph fragment를
  스스로 다시 발견하지 못하는 blind spot이 남아 있었다.

0044 방향:
- erase 강도를 무작정 올리지 않는다.
- 기존 0033 chromatic cleanup 뒤에
  "mask-independent flat residual verification" 단계를 추가한다.
- immutable RT-DETR TextBubble + FLAT_FILL target에만 적용한다.
- 기존 erase/glyph mask는 독립 검수의 입력 근거로 사용하지 않는다.
- 대신 다음 4가지 증거를 직접 비교한다.
  1. source image
  2. cleaned image
  3. immutable TextBounds
  4. BackgroundReconstructionV2가 이미 산출한 flat background color

판정:
- source pixel이 flat background에서 충분히 멀고
- cleaned pixel도 여전히 flat background에서 충분히 멀며
- cleaned pixel이 source pixel과 거의 같은 색으로 남아 있으면
  source-linked residual 후보로 본다.
- 긴 bubble border / panel rule 같은 구조가 지워지지 않도록
  TextBounds 대부분을 가로지르는 큰 connected component는 seed에서 제외한다.
- cleanup은 TextBounds 밖으로 절대 나가지 않는다.

cleanup:
- 안전 범위 안의 source-linked residual만
  저장된 FLAT_FILL background color로 다시 복원한다.
- cleanup 후 같은 독립 검사를 다시 실행한다.
- 독립 residual이 남으면 해당 TextRegionId를
  BackgroundQualityFailedTextRegionIds에 포함시켜 atomic commit을 차단한다.
- 즉 새 검수까지 실패하면 한국어를 겹쳐 쓰지 않고 원본 보존으로 돌아간다.

새 debug:
- *.v2_03e_independent_flat_residual.webp
- *.v2_03f_independent_flat_cleanup_mask.webp

audit:
- schema: pipeline-v2-erase-audit-v3
- IndependentFlatResidualCleanup
  * AppliedTargets
  * ResidualBeforePixels
  * CleanupMaskPixels
  * ResidualAfterPixels
  * FailedTargetIds
- background item별:
  * IndependentSourceLinkedResidualBeforePixels
  * IndependentSourceLinkedCleanupApplied
  * IndependentSourceLinkedCleanupPixels
  * IndependentSourceLinkedResidualAfterPixels
  * IndependentSourceLinkedResidualPass

PipelineChecks:
- 기존 erase mask가 놓친 검은 source glyph fragment를 독립 검수가 잡는지 확인.
- TextBounds 대부분을 가로지르는 긴 구조는 cleanup seed로 사용하지 않는지 확인.
- cleanup 후 source-linked residual이 0이 되는지 확인.
- geometry safety ceiling을 넘는 과도한 cleanup은 거부하는지 확인.

관련 commits:
- a287e4054576ad2675af9d97cfa0680201805614
  V2 0044 independent flat residual verifier
- 00227121b0a1a664abe6ed5f5daaba1357962638
  PipelineChecks
